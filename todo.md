# 📝 TODO

**Last Updated:** September 9, 2026 (register/POS frontend build: steps 1-5/9 done, login flow verified end-to-end against the real running stack; a separate DRY/SOLID/composition audit closed with 0 findings left open; two real production bugs found and fixed along the way — see Milestones 16-21)

A phase-by-phase log of what's been done on this repo and what's still open. This is the source of truth for progress.

**What this repo is:** a from-scratch personal demonstration workspace implementing the Distributed Saga pattern across independent microservices. This is not a fork or continuation of any existing project — the module boundaries and general shape of the problem are common territory for this class of system, but the code, design decisions, and tradeoffs recorded here are original. See [README.md](README.md) for the project description and [LICENSE](LICENSE) for licensing.

## 🏁 Milestones

The big checkpoints, distinct from the phase-by-phase log below — the story arc, not every step of it. Dates are the day the milestone's last piece actually landed, verified against real commits/PRs, not estimated.

| # | Milestone | Date | Detail |
| - | - | - | - |
| 1 | Repo bootstrap & tooling foundation | 2026-08-10 | Gradle over Maven, `io.github.terrence721.saga` namespace decided, CI (`quality.yml`/`codeql.yml`) green — Phases 1-3, 5 |
| 2 | `user-contract` + `user-service` complete | 2026-08-10 | Shared gRPC contract plus the identity/auth service, all 3 planned `user-service` test suites passing. Found and fixed a real production bug along the way: `spring-grpc`'s BOM was silently downgrading `protobuf-java` below what generated code needs, on the actual runtime classpath since Phase 7 — Phases 4, 7-9, 11-12, 14-15 |
| 3 | Consolidated test reporting live | 2026-08-10 | One aggregated HTML report across every module, committed and kept current by CI on every push, linked from the README — Phases 10, 13 |
| 4 | `order-service` complete | 2026-08-10 | The saga's entry point and sole owner of an order's lifecycle state. Found and fixed a real repo-wide risk: Spring Boot 3.4.1 couldn't boot at all under JDK 25 (not just a Gradle-plugin issue like Phase 8) — bumped to 3.5.16 across every Spring Boot module — Phases 16-25 |
| 5 | Local Docker infrastructure verified end-to-end | 2026-08-12 | Real Postgres + Kafka via `docker-compose.yml`; `order-service` booted against both with a genuine `HikariPool`→`PgConnection` connection and real Kafka partition assignments, not mocked — Phase 26 |
| 6 | `portfolio.html` published | 2026-08-12 | Case-study page live on GitHub Pages, built from this repo's own real test/architecture numbers, not aspirational copy |
| 7 | `payment-service` complete | 2026-08-12 | The saga's compensation half (charge on order creation, refund on rejection) wired end-to-end against real infra — Phases 27-31 |
| 8 | `restaurant-service` complete — full saga chain closes end-to-end | 2026-08-12 | order → payment → restaurant, with compensation flowing back on rejection. The core distributed-saga demonstration is functionally complete — Phases 32-36 |
| 9 | `api-gateway-service` complete — all 6 backend modules done | 2026-08-13 | JWT-guarded single entry point, gRPC-backed login, a Resilience4j circuit breaker on the downstream hop. Found a real bug in the source's own filter test along the way (asserted a status the filter never sets) — Phases 37-40 |
| 10 | Code-review audit launched — `user-contract` and `user-service` closed out same day | 2026-08-13 | Same file-by-file, one-PR-per-file discipline as `coolify-full`/`platform-main`. `user-contract` (1/1 files, 0 real bugs) and `user-service` (10/10 files, 5 real findings fixed including 2 real security issues — login enumeration and its timing-side-channel sibling) both complete — [#18](https://github.com/Terrence721/saga-full/issues/18), [#19](https://github.com/Terrence721/saga-full/issues/19) |
| 11 | Wiki + diagram pages launched | 2026-08-14 | 4 GitHub Pages diagram pages (architecture, saga flow, services reference, testing strategy) plus a 9-page thin wiki, linked from the README's **Start Here** section |
| 12 | `order-service` audit complete | 2026-08-19 | 15/15 files, 5 issues fixed, 0 findings left open — an IDOR fix plus 2 CodeQL log-injection follow-ups, a DTO-validation gap, a Kafka poison-pill gap, a second cross-service log-injection trace with a `customerId`/`ticketId` validation fix — [#20](https://github.com/Terrence721/saga-full/issues/20) |
| 13 | `payment-service` audit complete | 2026-08-25 | 14/14 files, 4 real findings fixed, 0 left open: a payment-decline path that never existed (`PaymentStatus.FAILED` was dead code), a refund compensation that never checked whose order it was refunding, a test-coverage gap on two idempotency-guard queries, and an unbounded Kafka-send timeout — the first of those cascaded a fix into `restaurant-service` ahead of that module's own audit turn — [#21](https://github.com/Terrence721/saga-full/issues/21) |
| 14 | `restaurant-service` audit complete | 2026-08-31 | 18/18 files, 5 real findings fixed, 0 left open: an unreachable inventory-seeding gap that meant no real order could ever succeed, a missing unique constraint that let duplicate Kafka delivery create two tickets for one order, an unbounded Kafka-send timeout and a missing `DefaultErrorHandler` — both the third occurrence of a gap already fixed in `order-service`/`payment-service` — and an unlocked stock-deduction race reachable once the service scales past one replica — [#22](https://github.com/Terrence721/saga-full/issues/22) |
| 15 | Code-review audit complete — all 6 modules | 2026-09-04 | `api-gateway-service` closed with 6 real findings fixed across 6 files (an event-loop-blocking gRPC call, two real CWE-117 log-injection fixes, and three test-coverage gaps) — [#23](https://github.com/Terrence721/saga-full/issues/23). **73/73 files reviewed audit-wide, 26 real findings fixed across 26 files, 0 findings left open** — [#17](https://github.com/Terrence721/saga-full/issues/17) |
| 16 | Test-coverage-gap scan complete — repo-wide floor reached | 2026-09-06 | A separate, deeper initiative than the audit above: a structural cross-reference of every main-source class against every test class, then a per-branch check for real branching logic. First pass (2026-09-04) found 7 real gaps/dead-code items across `CommonAppConfig`, `GrpcExecutor`, `OutboxPublisherService`×3, `RestaurantService`, `DependencyUnavailableException`. Resumed 2026-09-06 and found 2 more — `AuthenticationController`'s unreachable `.defaultIfEmpty(...)` branch and `JwtPerimeterGuardGatewayFilterFactory`'s untested non-Bearer-header branch — before confirming every remaining class had already been through either the audit or this scan's per-branch pass. **9 real findings total**, 161/161 tests |
| 17 | CI speed applied, then a real regression it caused found and fixed same day | 2026-09-06 | Root `gradle.properties` (`org.gradle.caching=true` + `org.gradle.parallel=true`) — [#47](https://github.com/Terrence721/saga-full/issues/47). Enabling parallel execution surfaced a genuine `WebTestClient` timeout flake under CPU contention, fixed via explicit `@AutoConfigureWebTestClient` timeouts. Enabling the build cache then broke CodeQL repo-wide once `main`'s cache warmed up — `could not process any code written in Java/Kotlin`, since a cache hit meant `javac` never actually ran for CodeQL's tracer to observe — found via real CI run history and fixed with `--no-build-cache` scoped to just that workflow step — [#197](https://github.com/Terrence721/saga-full/issues/197) |
| 18 | Full Docker containerization — all 5 services + compose | 2026-09-06 | Multi-stage `Dockerfile`s for every service, `docker-compose.yml` wired end-to-end — [#14](https://github.com/Terrence721/saga-full/issues/14). Two real bugs found only by actually running the 7-container stack: Kafka's single advertised listener didn't work for container-to-container connections (fixed with the standard dual-listener pattern), and the gateway's circuit breaker had no explicit timeout, silently defaulting to 1s — too tight for a cold cross-container call (a real, measured 1.29s). Verified end-to-end including the saga's compensation path, not just the happy path |
| 19 | Register/POS frontend build launched | 2026-09-06 | Full 9-step plan approved for [#15](https://github.com/Terrence721/saga-full/issues/15) (React/TS/Vite/Tailwind, SSE streamed from `order-service` and proxied by the gateway — not a new Kafka consumer on the gateway). Step 1 of 9 done: `GET /orders/{id}` added to `order-service`, order-service's first-ever HTTP exception handler — [#199](https://github.com/Terrence721/saga-full/issues/199) |
| 20 | Architectural DRY/SOLID/composition audit complete | 2026-09-09 | A separate lens from the correctness/security/test-coverage audit ([#17](https://github.com/Terrence721/saga-full/issues/17)): found real DRY and SOLID issues across `order-service`/`payment-service`/`restaurant-service` — triplicated outbox-record-building logic extracted into a per-service `OutboxRecordFactory`, `order-service`'s SSE-push responsibility split out of `OrderService` into a new `OrderUpdatePublisher`, and each module's `*ConsumerConfig` retry-policy bean split into its own `KafkaErrorHandlerConfig`. 7 new files, 0 findings left open — [#207](https://github.com/Terrence721/saga-full/issues/207)/[PR #208](https://github.com/Terrence721/saga-full/pull/208) |
| 21 | Register/POS frontend: steps 2-5/9 done — live SSE status, gateway routing, and a working login flow, all verified end-to-end against the real running stack | 2026-09-09 | `order-service` gained a live SSE stream ([#201](https://github.com/Terrence721/saga-full/issues/201)/[PR #202](https://github.com/Terrence721/saga-full/pull/202)) plus a real stale-buffered-replay bug found and fixed the same day ([#203](https://github.com/Terrence721/saga-full/issues/203)/[PR #204](https://github.com/Terrence721/saga-full/pull/204)). `api-gateway-service` gained the new GET/stream routes plus CORS, with a real Gateway preflight-routing bug found and fixed along the way ([#205](https://github.com/Terrence721/saga-full/issues/205)/[PR #206](https://github.com/Terrence721/saga-full/pull/206)). The Vite/React/TypeScript/Tailwind shell was scaffolded on Yarn ([#209](https://github.com/Terrence721/saga-full/issues/209)/[PR #210](https://github.com/Terrence721/saga-full/pull/210)), and a full login flow was built and wired in — `httpClient`, `authClient`, `AuthContext`, `LoginForm`, `App` wiring ([#211](https://github.com/Terrence721/saga-full/issues/211)/[#213](https://github.com/Terrence721/saga-full/issues/213)/[#215](https://github.com/Terrence721/saga-full/issues/215), PRs [#212](https://github.com/Terrence721/saga-full/pull/212)/[#214](https://github.com/Terrence721/saga-full/pull/214)/[#216](https://github.com/Terrence721/saga-full/pull/216)/[#217](https://github.com/Terrence721/saga-full/pull/217)/[#218](https://github.com/Terrence721/saga-full/pull/218)) — a real cross-platform file-casing bug (Windows/macOS case-insensitive filesystems vs. case-sensitive TypeScript compilation) was found and fixed along the way. A second real bug, this one only reachable once real browser verification was possible: `/auth/login`'s own `@RestController` dispatch bypassed the gateway's route-scoped CORS config entirely, rejecting the frontend's real preflight with 403 — fixed with a `HandlerMapping`-agnostic `CorsWebFilter` bean, replacing the route-scoped config ([PR #219](https://github.com/Terrence721/saga-full/pull/219)). The same PR fixed a real Dockerfile inefficiency found while iterating on that bug: no BuildKit Gradle cache mount meant every rebuild re-downloaded the Gradle distribution and re-resolved every dependency from scratch (10+ minutes observed) — fixed across all 5 service `Dockerfile`s. Login now works end-to-end against the real gateway, not just against mocks. 181/181 tests passing |

## At a glance

**Done, in full:**

| Item | Detail | Phases |
| - | - | - |
| Repo bootstrap | git init, README, MIT LICENSE (Terrence Daniels), docs/ skeletons, .gitignore, CONTRIBUTING.md | Phase 1 |
| Build tool: Gradle | Chosen over Maven (source's own tool) for incremental/cached builds across this six-module project — a build-time win, not a runtime one; reasoning in [docs/architecture.md](docs/architecture.md) | Phase 2 |
| Root namespace: `io.github.terrence721.saga` | Anchors the package/group ID to an identity actually owned (this repo's GitHub account) rather than an unowned domain like the first proposal, `com.sagafull.saga`; reasoning in [docs/architecture.md](docs/architecture.md) | Phase 3 |
| `user-contract` module | `settings.gradle.kts`, `build.gradle.kts`, and an original `user.proto` (gRPC login + token validation contract) written; `./gradlew :user-contract:build` verified green end-to-end, including a real Gradle-version incompatibility and a real missing-symbol compile error found and fixed along the way, not just written on faith; reasoning in [docs/architecture.md](docs/architecture.md) | Phase 4 |
| CI: quality + CodeQL | `quality.yml` (build/test jobs) and `codeql.yml` (java-kotlin) added; first real run failed (`gradlew` committed without its executable bit from Windows), fixed and confirmed green on a second real run; reasoning in [docs/architecture.md](docs/architecture.md) | Phase 5 |
| `user-service` core | Entry point, config, `User` entity (UUID id), `UserRepository`, exception types, `JwtTokenProvider`, and `UserGrpcServiceImpl` (`Login` + `ValidateToken`) written; hit and fixed a real Lombok/JDK 25 incompatibility along the way; `./gradlew :user-service:compileJava` verified green; reasoning in [docs/architecture.md](docs/architecture.md) | Phase 7 |
| CI fix: `resolveMainClassName` | First real CI run of `quality.yml` against `user-service` failed (`Unsupported class file major version 69` — Spring Boot Gradle plugin's bundled ASM can't read JDK 25 bytecode). Fixed with an explicit `springBoot { mainClass.set(...) }`, skipping the auto-detection scan entirely; reproduced the failure locally and confirmed both `./gradlew assemble` and `./gradlew test` green before pushing; reasoning in [docs/architecture.md](docs/architecture.md) | Phase 8 |
| `user-contract` tests | JUnit 5.11.4 + AssertJ 3.26.3 added (pinned to match what Spring Boot manages for `user-service`, not the new JUnit 6.1.3); 6 serialization round-trip tests written since there's no hand-written Java to unit test in a proto-only module; verified genuinely green with a real `./gradlew :user-contract:test` run, and verified the suite actually catches a wrong value by deliberately breaking one assertion and re-running before reverting; reasoning in [docs/architecture.md](docs/architecture.md) | Phase 9 |
| Consolidated test report | Custom root `aggregateTestReport` task merges every module's JUnit XML into one HTML file (`build/reports/tests/aggregate/index.html`), auto-picking up future modules with no edits needed. Gradle's built-in `test-report-aggregation` plugin was tried first and abandoned — it needs every module's full dependency graph (Spring Boot's BOM, etc.) resolvable from the root, an ongoing maintenance burden. Caught and fixed two real bugs along the way: a failing test silently blocking the report from generating at all, and a stale-report bug (no declared task inputs, so Gradle skipped regenerating it). Wired into CI: `quality.yml`'s `Test` job now runs it and uploads the merged report as a downloadable GitHub Actions artifact on every run. Reasoning in [docs/architecture.md](docs/architecture.md), usage in [CONTRIBUTING.md](CONTRIBUTING.md) | Phase 10 |
| `user-service` tests (complete) | `JwtTokenProviderTest` (5), `UserGrpcServiceImplTest` (2, happy path), `UserGrpcServiceImplErrorTest` (4, error path), and `UserGrpcServiceIntegrationTest` (3, real in-process gRPC wire calls) — all 14 tests written and passing. Found and fixed a real Mockito/JDK 25 incompatibility, and — far more significant — a **real production bug**: `spring-grpc`'s BOM was silently downgrading `protobuf-java` below what `user-contract`'s generated code needs, on `user-service`'s actual runtime classpath, since Phase 7. It went undetected because nothing before this test ever actually constructed a generated message at runtime. Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 11-12, 14-15 |
| README test-status visibility | Quality/CodeQL CI badges, an "At a glance" test-count line, and a committed `test-report.html` linked directly from the README — kept current by a new `quality.yml` step that commits it back to `main` after every test run. GitHub Pages briefly switched to Actions-based deployment to try matching `platform-main`'s live-Pages pattern, then deliberately reverted in favor of this simpler committed-file approach. Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 13 |
| `order-service` scaffold | `settings.gradle.kts` registration, `build.gradle.kts` (spring-kafka direct instead of the source's Spring Cloud Stream binder, per a deliberate deviation), and the `OrderServiceApplication` entry point; `./gradlew :order-service:compileJava` verified green. Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 16 |
| **Real risk found and fixed:** Spring Boot 3.4.1 can't boot under JDK 25 | `order-service`'s first test (`contextLoads`) failed outright — Spring Framework's own bundled ASM (not just the Gradle plugin's, per Phase 8) can't parse JDK 25 class files at all during component scanning. Checking `user-service` directly showed it had never once booted a real `ApplicationContext` in any test either, an identical-in-shape gap to the Phase 12 protobuf-java bug. Fixed by bumping **both** modules from Boot 3.4.1 to 3.5.16 — verified with a real `contextLoads()` pass in each, `user-service`'s showing an actual gRPC server starting and stopping. Every future Spring Boot module should start on 3.5.16. Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 17 |
| `order-service` service layer + Kafka wiring | `OrderService` (`createOrder`/`confirmOrder`/`cancelOrder`, `OrderNotFoundException`), `OutboxPublisherService` (`KafkaTemplate` poller, not the source's `StreamBridge`), `OrderController` (`201 Created`, no perimeter-header trust boundary yet), `OrderConsumerConfig` (`@KafkaListener`, not Cloud Stream), and `application.yaml` (`default`/`postgres` profiles) — 15 new tests across 4 suites, all with real verified runs. Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 21-25 |
| Docker: `docker-compose.yml` (Postgres + Kafka) | `postgres-db` + `kafka-broker` only — no per-service containers yet. Verified for real: `order-service` actually booted against both containers (`bootRun` with the `postgres` profile), a genuine `HikariPool`→`PgConnection` connection, and both `@KafkaListener` consumer groups joining and getting real partition assignments against the live broker. Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 26 |
| `payment-service` scaffold | `build.gradle.kts` (mirrors `order-service`'s Phase 16 shape: `spring-kafka` direct, Boot 3.5.16 from the start, Lombok 1.18.42 pinned), `PaymentServiceApplication` entry point, and `contextLoads()` written immediately — the actual next link in the saga chain (consumes `OrderCreatedEvent`, not `restaurant-service`, which depends on `payment-service`'s own output). Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 27 |
| `payment-service` domain model | `Payment` (`UUID` id/`orderId`, `BigDecimal` amount — same conventions as `order-service`'s Phase 18, not a new decision), `PaymentStatus` (`APPROVED`/`REFUNDED`/`FAILED`, unchanged from the source), `OutboxRecord` (same shape as `order-service`'s, no trace columns). Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 28 |
| `payment-service` repositories | `PaymentRepository` (`findByOrderId`) and `OutboxRepository` (`SKIP LOCKED` query carried over unchanged), both re-typed to `UUID` matching Phase 28 — same shape as `order-service`'s Phase 19. 4 new tests, all real `@DataJpaTest`s against embedded H2. Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 29 |
| `payment-service` DTOs + `PaymentService` | 3 DTOs (`OrderCreatedEvent`, `PaymentProcessedEvent`, `RestaurantRejectedEvent` — 2 dead source DTOs dropped, `OrderStatus`'s drift from `order-service`'s real enum corrected), `PaymentService` (`processPaymentSaga`/`handleOrderCompensation`, matching `order-service`'s idempotency-guard shape), new `PaymentNotFoundException`. 6 new tests, real `ObjectMapper`. Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 30 |
| `payment-service` Kafka wiring | `OutboxPublisherService`, `PaymentConsumerConfig` (`@KafkaListener`), `application.yaml` (`default`/`postgres` profiles) — direct structural repeats of `order-service`'s Phase 22/24/25, no new forks. 6 new tests. Verified end-to-end against real Postgres + Kafka: genuine `HikariPool`→`PgConnection`, both consumer groups joining and getting real partition assignments against the live broker. Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 31 |
| `restaurant-service` scaffold | `build.gradle.kts` (same shape as `payment-service`'s Phase 27 — third repeat of an established pattern, no new decisions), `RestaurantServiceApplication` entry point, `contextLoads()` written immediately. Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 32 |
| `restaurant-service` domain model | `RestaurantTicket` (`UUID` id/`orderId`, matching `order-service`'s `RestaurantApprovedEvent.ticketId` typing), `InventoryItem` (natural-key `String itemCode` — the one entity in this repo that doesn't fit the `UUID` convention, deliberately), `InventoryStatus`, `RestaurantTicketStatus` (the real, actively-used enum — not a dead copy like the ones dropped from `order-service`/`payment-service`'s DTOs), `OutboxRecord`. Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 33 |
| `restaurant-service` repositories | `InventoryItemRepository` (no custom query needed), `RestaurantTicketRepository` (`existsByOrderId`), `OutboxRepository` (`SKIP LOCKED`) — same shape as `order-service`/`payment-service`'s, no new decisions. 6 new tests, all real `@DataJpaTest`s. Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 34 |
| `restaurant-service` DTOs + `RestaurantService` | 5 DTOs, all genuinely used (no dead code this time — includes a sealed `RestaurantEvent` interface). `RestaurantService.processRestaurantStep` keeps real cross-service message validation but drops the one unreachable `status != APPROVED` check, matching Phase 30's precedent. New `RestaurantInventoryServiceTest` goes beyond the source. 9 new tests. Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 35 |
| `restaurant-service` Kafka wiring | `OutboxPublisherService` (routes to one of **two** topics by `OutboxRecord.eventType`, not deserialization — the one real fork from `order-service`/`payment-service`'s single-topic shape), `RestaurantConsumerConfig`, `application.yaml`. 6 new tests. Verified end-to-end against real Postgres + Kafka. **Closes the full saga chain end-to-end.** Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 36 |
| `api-gateway-service` | Reactive WebFlux/Spring Cloud Gateway module (Spring Cloud 2025.0.0, this repo's first non-servlet stack) — `AuthenticationController`/`UserGrpcClient` proxy `POST /auth/login` to `user-service`'s real `Login` gRPC RPC, `JwtPerimeterGuardGatewayFilterFactory` guards `POST /orders` using the same signing secret as `user-service`'s `JwtTokenProvider`, a Resilience4j circuit breaker falls back to `GatewayFallbackController` on downstream failure. Found and fixed a real bug in the source's own filter test (asserted a response status the filter never sets). 7 new tests, `./gradlew :api-gateway-service:test` verified green; full 88-test multi-module suite also green. **Completes all five originally-planned backend modules.** Reasoning in [docs/architecture.md](docs/architecture.md) | Phase 37-40 |
| `portfolio.html` | Case-study page for the portfolio site, written from this repo's own real test/architecture numbers (test-coverage ledger, the At-a-glance table's labeled real-bug findings) | (standalone, between Phase 36 and 37) |

**Actually still open, right now:** the register/POS frontend build (in progress — steps 1-5/9 done, login flow verified end-to-end) and an unscoped `reservation-service` addition. The code-review audit (all 6 modules, 73/73 files), the separate test-coverage-gap scan, the DRY/SOLID/composition audit, and full Docker containerization are all complete. See the **Still to do** table below.

## 🧪 Test Coverage Ledger

Every test suite added to this repo, and its last confirmed real run. Updated as suites are added or re-run — this table only reflects an actual `./gradlew :<module>:test` run, never an assumed result.

| Module | Test class | Tests | Result | Phase | Date |
| - | - | - | - | - | - |
| `user-contract` | `UserContractSerializationTest` | 6 | ✅ passing | Phase 9 | 2026-08-10 |
| `user-service` | `JwtTokenProviderTest` | 5 | ✅ passing | Phase 11 | 2026-08-10 |
| `user-service` | `UserGrpcServiceImplTest` | 2 | ✅ passing | Phase 12 | 2026-08-10 |
| `user-service` | `UserGrpcServiceImplErrorTest` | 6 | ✅ passing | [#41](https://github.com/Terrence721/saga-full/pull/41) | 2026-08-13 |
| `user-service` | `GrpcExecutorTest` | 1 | ✅ passing | [#185](https://github.com/Terrence721/saga-full/pull/185) | 2026-09-04 |
| `user-service` | `UserGrpcServiceIntegrationTest` | 3 | ✅ passing | Phase 15 | 2026-08-10 |
| `user-service` | `UserServiceApplicationTests` | 1 | ✅ passing | Phase 17 | 2026-08-10 |
| `user-service` | `SecurityConfigTest` | 2 | ✅ passing | [#29](https://github.com/Terrence721/saga-full/pull/29) | 2026-08-13 |
| `user-service` | `UserRepositoryTest` | 3 | ✅ passing | [#45](https://github.com/Terrence721/saga-full/pull/45) | 2026-08-13 |
| `order-service` | `OrderServiceApplicationTests` | 1 | ✅ passing | Phase 16-17 | 2026-08-10 |
| `order-service` | `OrderRepositoryTest` | 3 | ✅ passing | Phase 19 | 2026-08-10 |
| `order-service` | `OutboxRepositoryTest` | 2 | ✅ passing | Phase 19 | 2026-08-10 |
| `order-service` | `OrderServiceTest` | 12 | ✅ passing | [#208](https://github.com/Terrence721/saga-full/pull/208) | 2026-09-09 |
| `order-service` | `OutboxPublisherServiceTest` | 6 | ✅ passing | [#186](https://github.com/Terrence721/saga-full/pull/186) | 2026-09-04 |
| `order-service` | `OrderControllerTest` | 14 | ✅ passing | [#202](https://github.com/Terrence721/saga-full/pull/202) | 2026-09-09 |
| `order-service` | `OrderConsumerConfigTest` | 2 | ✅ passing | [#208](https://github.com/Terrence721/saga-full/pull/208) | 2026-09-09 |
| `order-service` | `OutboxRecordFactoryTest` | 2 | ✅ passing | [#208](https://github.com/Terrence721/saga-full/pull/208) | 2026-09-09 |
| `order-service` | `OrderUpdatePublisherTest` | 3 | ✅ passing | [#208](https://github.com/Terrence721/saga-full/pull/208) | 2026-09-09 |
| `order-service` | `KafkaErrorHandlerConfigTest` | 3 | ✅ passing | [#208](https://github.com/Terrence721/saga-full/pull/208) | 2026-09-09 |
| `payment-service` | `PaymentServiceApplicationTests` | 1 | ✅ passing | Phase 27 | 2026-08-12 |
| `payment-service` | `PaymentRepositoryTest` | 7 | ✅ passing | [#103](https://github.com/Terrence721/saga-full/pull/103) | 2026-08-25 |
| `payment-service` | `OutboxRepositoryTest` | 2 | ✅ passing | Phase 29 | 2026-08-12 |
| `payment-service` | `PaymentServiceTest` | 9 | ✅ passing | [#97](https://github.com/Terrence721/saga-full/pull/97) | 2026-08-22 |
| `payment-service` | `OutboxPublisherServiceTest` | 6 | ✅ passing | [#187](https://github.com/Terrence721/saga-full/pull/187) | 2026-09-04 |
| `payment-service` | `PaymentConsumerConfigTest` | 2 | ✅ passing | [#208](https://github.com/Terrence721/saga-full/pull/208) | 2026-09-09 |
| `payment-service` | `OutboxRecordFactoryTest` | 2 | ✅ passing | [#208](https://github.com/Terrence721/saga-full/pull/208) | 2026-09-09 |
| `payment-service` | `KafkaErrorHandlerConfigTest` | 3 | ✅ passing | [#208](https://github.com/Terrence721/saga-full/pull/208) | 2026-09-09 |
| `restaurant-service` | `RestaurantServiceApplicationTests` | 2 | ✅ passing | [#111](https://github.com/Terrence721/saga-full/pull/111) | 2026-08-25 |
| `restaurant-service` | `InventoryItemRepositoryTest` | 2 | ✅ passing | Phase 34 | 2026-08-12 |
| `restaurant-service` | `RestaurantTicketRepositoryTest` | 3 | ✅ passing | [#117](https://github.com/Terrence721/saga-full/pull/117) | 2026-08-28 |
| `restaurant-service` | `OutboxRepositoryTest` | 2 | ✅ passing | Phase 34 | 2026-08-12 |
| `restaurant-service` | `RestaurantServiceTest` | 11 | ✅ passing | [#189](https://github.com/Terrence721/saga-full/pull/189) | 2026-09-04 |
| `restaurant-service` | `RestaurantInventoryServiceTest` | 3 | ✅ passing | Phase 35 | 2026-08-12 |
| `restaurant-service` | `RestaurantInventoryServiceConcurrencyTest` | 1 | ✅ passing | [#146](https://github.com/Terrence721/saga-full/pull/146) | 2026-08-31 |
| `restaurant-service` | `OutboxPublisherServiceTest` | 7 | ✅ passing | [#188](https://github.com/Terrence721/saga-full/pull/188) | 2026-09-04 |
| `restaurant-service` | `RestaurantConsumerConfigTest` | 1 | ✅ passing | [#208](https://github.com/Terrence721/saga-full/pull/208) | 2026-09-09 |
| `restaurant-service` | `OutboxRecordFactoryTest` | 2 | ✅ passing | [#208](https://github.com/Terrence721/saga-full/pull/208) | 2026-09-09 |
| `restaurant-service` | `KafkaErrorHandlerConfigTest` | 2 | ✅ passing | [#208](https://github.com/Terrence721/saga-full/pull/208) | 2026-09-09 |
| `api-gateway-service` | `ApiGatewayServiceApplicationTests` | 1 | ✅ passing | Phase 37-40 | 2026-08-13 |
| `api-gateway-service` | `AuthenticationControllerTest` | 8 | ✅ passing | [#164](https://github.com/Terrence721/saga-full/pull/164) | 2026-09-02 |
| `api-gateway-service` | `JwtPerimeterGuardGatewayFilterFactoryTest` | 5 | ✅ passing | [#194](https://github.com/Terrence721/saga-full/pull/194) | 2026-09-06 |
| `api-gateway-service` | `JwtPerimeterGuardIntegrationTest` | 4 | ✅ passing | [#206](https://github.com/Terrence721/saga-full/pull/206) | 2026-09-09 |
| `api-gateway-service` | `AuthenticationControllerThreadingTest` | 1 | ✅ passing | [#152](https://github.com/Terrence721/saga-full/pull/152) | 2026-08-31 |
| `api-gateway-service` | `GatewayFallbackControllerTest` | 1 | ✅ passing | [#154](https://github.com/Terrence721/saga-full/pull/154) | 2026-08-31 |
| `api-gateway-service` | `UserGrpcClientTest` | 3 | ✅ passing | [#174](https://github.com/Terrence721/saga-full/pull/174) | 2026-09-04 |
| `api-gateway-service` | `UserGrpcExceptionTranslatorTest` | 8 | ✅ passing | [#176](https://github.com/Terrence721/saga-full/pull/176) | 2026-09-04 |
| `api-gateway-service` | `CommonAppConfigTest` | 1 | ✅ passing | [#178](https://github.com/Terrence721/saga-full/pull/178) | 2026-09-04 |
| `api-gateway-service` | `GlobalCorsConfigTest` | 4 | ✅ passing | [#219](https://github.com/Terrence721/saga-full/pull/219) | 2026-09-09 |

## ✅ Done

### Repository bootstrap

| Phase | Date | What |
| - | - | - |
| 1 | 2026-08-10 | `git init`; README.md written describing the project and its scope; MIT LICENSE added under Terrence Daniels; `docs/architecture.md` and `docs/case-study.md` skeletons added; `.gitignore` added; `CONTRIBUTING.md` added |
| 2 | 2026-08-10 | Build tool decided: Gradle (Kotlin DSL) over Maven, for incremental compilation and build caching across this six-module project — faster local/CI build turnaround, with no effect on the running services' own performance. `.gitignore` and `CONTRIBUTING.md` updated from Maven to Gradle conventions; full reasoning in [docs/architecture.md](docs/architecture.md) |
| 3 | 2026-08-10 | Root package/group namespace decided: `io.github.terrence721.saga`, after ruling out `com.sagafull.saga` (implies ownership of an unowned domain) in favor of the GitHub-identity convention. Full reasoning in [docs/architecture.md](docs/architecture.md) |

### `user-contract` module

| Phase | Date | What |
| - | - | - |
| 4 | 2026-08-10 | Root `settings.gradle.kts` written, including `user-contract`. `user-contract/build.gradle.kts` written (Java 25 toolchain, `com.google.protobuf` plugin 0.9.4, grpc/protobuf deps exposed via `api`). `user.proto` written from scratch — `UserIdentityService` with `Login` and `ValidateToken` RPCs, deliberately not a field-for-field mirror of the source's single-RPC contract. JDK 25 (Eclipse Temurin) and a Gradle wrapper installed/generated; hit and fixed a real Gradle 8.11/JDK 25 Kotlin-DSL incompatibility (moved to Gradle 9.7.0) and a real missing-`javax.annotation.Generated` compile error (added `javax.annotation-api` as `compileOnly`). `./gradlew :user-contract:build` verified green. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 9 | 2026-08-10 | `user-contract/build.gradle.kts` gained JUnit 5.11.4, AssertJ 3.26.3, and `junit-platform-launcher` (also added to `user-service`, missing from both). `UserContractSerializationTest` written: 6 serialize/parse round-trip tests covering all 4 message types plus an int64 boundary case, since there's no hand-written Java in a proto-only module to unit test otherwise. Verified with a real `./gradlew :user-contract:test` run, and confirmed the suite genuinely catches wrong data by deliberately breaking one assertion and re-running before reverting it. Full reasoning in [docs/architecture.md](docs/architecture.md) |

### CI

| Phase | Date | What |
| - | - | - |
| 5 | 2026-08-10 | `.github/workflows/quality.yml` (parallel `build`/`test` jobs, JDK 25 Temurin, `gradle/actions/setup-gradle`) and `codeql.yml` (java-kotlin, real build before analyze) added. First real push failed both workflows: `./gradlew: Permission denied`, exit 126 — `gradlew` was committed from Windows without its executable bit, so git stored mode `100644` instead of `100755`. Fixed with `git update-index --chmod=+x gradlew`; a second real run confirmed both workflows green. Full reasoning in [docs/architecture.md](docs/architecture.md) |

### `user-service` module

| Phase | Date | What |
| - | - | - |
| 7 | 2026-08-10 | `settings.gradle.kts` updated to include `user-service`; `build.gradle.kts` written (actuator, data-jpa, spring-grpc-server, spring-security-crypto instead of the source's unmaintained `jbcrypt`, java-jwt, H2/Postgres runtime drivers). `UserServiceApplication` entry point; `application.yaml` (default H2 / `postgres` profiles, no baked-in JWT secret default); `User` entity with a Hibernate-native `UUID` id instead of the source's un-generated `Long`; `UserRepository`; `UserNotFoundException`/`InvalidCredentialsException`/`UserInactiveException`; `SecurityConfig` exposing a `BCryptPasswordEncoder` bean; `GrpcExecutor` (domain-exception → gRPC `Status` mapping, using `java.util.function.Supplier` instead of the source's Guava dependency); `JwtTokenProvider` (token creation and verification split out into its own component, unlike the source, which never actually implemented token validation); `UserGrpcServiceImpl` implementing both `Login` and `ValidateToken` — the latter returns `valid: false` rather than a gRPC error on a bad token. Hit and fixed a real Lombok 1.18.36/JDK 25 incompatibility (`NoSuchFieldException: TypeTag :: UNKNOWN`), pinned to 1.18.42. `./gradlew :user-service:compileJava` verified green. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 8 | 2026-08-10 | First real CI run against `user-service` failed at `:user-service:resolveMainClassName` (`Unsupported class file major version 69`) — Spring Boot 3.4.1's Gradle plugin bundles an ASM that predates JDK 25 and can't read its bytecode when auto-detecting the main class. Fixed with an explicit `springBoot { mainClass.set(...) }` in `user-service/build.gradle.kts`, skipping the scan. Reproduced the exact failure locally with `./gradlew assemble`, confirmed the fix with a second real run, and confirmed `./gradlew test` also passes clean with no tests yet. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 11 | 2026-08-10 | `JwtTokenProviderTest` written: 5 tests, no Spring context needed since `JwtTokenProvider` takes plain constructor args. Covers a created token verifying successfully with matching claims, and `verifyToken` returning empty for a tampered signature, wrong issuer, expired token, and malformed input. Verified with a real `./gradlew :user-service:test` run, and confirmed the suite genuinely catches wrong data by deliberately breaking one assertion and re-running before reverting it. |
| 12 | 2026-08-10 | `UserGrpcServiceImplTest` written (happy-path: `login` succeeds and returns an access token, `validateToken` returns `valid: true` for a verifying token), using a hand-written `RecordingStreamObserver` instead of the deprecated `io.grpc.testing.StreamRecorder` (no official replacement upstream). Hit and fixed two real issues along the way: Mockito 5.14.2's bundled ByteBuddy 1.15.11 can't mock on JDK 25 (pinned mockito-core/mockito-junit-jupiter to 5.23.0 and byte-buddy to 1.17.7, since Spring's BOM kept forcing byte-buddy back down even after the mockito bump); and a **real production bug** — `spring-grpc`'s BOM was silently downgrading `protobuf-java` to 3.25.6 on `user-service`'s actual runtime classpath (not just tests) since Phase 7, below what `user-contract`'s generated code needs, undetected until now because nothing before this test ever constructed a generated message at runtime. Fixed with an explicit `implementation("com.google.protobuf:protobuf-java:4.28.2")`. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 14 | 2026-08-10 | `UserGrpcServiceImplErrorTest` written: 4 tests covering `login`'s three error branches (`NOT_FOUND` for an unknown email, `PERMISSION_DENIED` for an inactive user, `UNAUTHENTICATED` for a wrong password, each asserted via `Status.fromThrowable(...).getCode()`) and `validateToken`'s failure branch (`valid: false`, no exception, for a token that fails verification). Reuses `RecordingStreamObserver` from Phase 12. Verified with a real `./gradlew :user-service:test` run, and confirmed the suite genuinely catches a wrong status code by deliberately swapping one assertion's expected code and re-running before reverting it. |
| 15 | 2026-08-10 | `UserGrpcServiceIntegrationTest` written: 3 tests running a real in-process `Server`/`ManagedChannel` pair (`.directExecutor()` on both, grpc-java's own recommended test pattern) and calling through the actual generated `UserIdentityServiceGrpc` blocking stub, not a captured `StreamObserver` — the one thing the other two suites structurally can't prove: that messages and `Status` codes survive real (de)serialization. Hit a real compile-vs-runtime dependency-scope gap: `grpc-inprocess` was already on `testRuntimeClasspath` (transitive via `grpc-testing`) but absent from `testCompileClasspath`, since the upstream POM scopes it `runtime`-only; added explicitly as `testImplementation`. Verified with a real `./gradlew :user-service:test` run, and confirmed the suite genuinely catches a wrong status code the same way as Phase 14's suite. This completes all three planned `user-service` test suites. Full reasoning in [docs/architecture.md](docs/architecture.md) |

### README test-status visibility

| Phase | Date | What |
| - | - | - |
| 13 | 2026-08-10 | Quality/CodeQL CI badges and an "At a glance" test-count line added to README.md, linking to a new committed `test-report.html` at the repo root instead of a markdown ledger buried in `todo.md`. `quality.yml`'s `Test` job copies the aggregated report there and commits it back to `main` after every real push (guarded to only actual pushes to `main`, only when the file changed, `[skip ci]` to avoid retriggering CI). GitHub Pages was briefly switched to GitHub Actions-based deployment to try matching `platform-main`'s live-rendered-site pattern (a `pages.yml` workflow using `upload-pages-artifact`/`deploy-pages`), then deliberately reverted back to the legacy branch build in favor of the simpler committed-static-file approach, which doesn't replace the README-rendered homepage. A real anomaly, since confirmed one-off: force-pushing the folded commit didn't trigger `quality.yml`/`codeql.yml` automatically (verified instead via a manual `workflow_dispatch` run), but the next genuine push triggered both normally and produced a real `chore: update consolidated test report [skip ci]` auto-commit — the actual end-to-end proof this mechanism works. Full reasoning in [docs/architecture.md](docs/architecture.md) |

### Consolidated test reporting

| Phase | Date | What |
| - | - | - |
| 10 | 2026-08-10 | Tried Gradle's built-in `test-report-aggregation` plugin at the root project first; abandoned after it required resolving every subproject's full dependency graph from the root (missing repositories, then unresolvable Spring-managed versions) — an ongoing burden that would grow with every future module. Replaced with a custom root `build.gradle.kts` task, `aggregateTestReport`, that reads each module's already-written JUnit XML with the JDK's own XML parser (zero extra dependencies) and merges it into one HTML file. Caught and fixed two real bugs via actual runs: a failing test blocking the report from generating at all (fixed by decoupling `aggregateTestReport` from the `test` tasks — documented two-step workflow in [CONTRIBUTING.md](CONTRIBUTING.md)), and a stale-report bug where the task had no declared inputs and Gradle silently kept serving an outdated report (fixed by declaring each module's `test-results/test` directory as an input once it exists). Full reasoning in [docs/architecture.md](docs/architecture.md) |

### `order-service` module

| Phase | Date | What |
| - | - | - |
| 16 | 2026-08-10 | `settings.gradle.kts` updated to include `order-service`; `build.gradle.kts` written (actuator, validation, web, data-jpa, H2/Postgres runtime drivers, Lombok pinned to 1.18.42 matching `user-service`). Deliberately used `spring-kafka` directly instead of the source's Spring Cloud Stream Kafka binder — this project only ever targets Kafka, never needs broker-swappability, so the extra abstraction wasn't worth the added functional-binding config. OTel export and a Kubernetes-specific datasource profile were both deliberately deferred: the source's config points at infrastructure (an OTLP collector, a Minikube-hosted Postgres) this repo doesn't have yet. `OrderServiceApplication` entry point (with `@EnableScheduling`, needed later for outbox polling). `./gradlew :order-service:compileJava` verified green. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 17 | 2026-08-10 | `OrderServiceApplicationTests.contextLoads()` written as the module's first test — and it immediately failed for real: Spring Boot 3.4.1's Spring Framework can't parse JDK 25 class files during component scanning at all (`Unsupported class file major version 69`), a deeper issue than Phase 8's Gradle-plugin-scoped one. Checking `user-service` showed it had the identical latent risk, undetected because no test there had ever booted a real `ApplicationContext`. Fixed by bumping **both** modules' Spring Boot plugin version from 3.4.1 to 3.5.16 — verified with `order-service`'s test passing (real JPA/Hikari boot) and a new `UserServiceApplicationTests.contextLoads()` added to `user-service` and passing (real gRPC server + JPA/Hikari boot, the first proof that module starts at all under JDK 25). Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 18 | 2026-08-10 | `order-service` domain model written: `OrderStatus` (`PENDING`/`CANCELLED`/`SUCCESS`, unchanged from the source), `Order` (`UUID id`/`customerId` per `user-service`'s Phase 7 precedent, `BigDecimal totalAmount` instead of the source's `double` — floating point can't exactly represent decimal fractions, a real correctness concern for money), and `OutboxRecord` (matches the source, minus its `traceId`/`spanId` columns — deferred along with OTel export wiring itself, per Phase 16, rather than adding trace-context columns with no tracer to populate them). `./gradlew :order-service:compileJava` and the full 22-test suite both verified green with no regression. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 19 | 2026-08-10 | `order-service` repositories written: `OrderRepository` (`existsByIdAndStatus`) and `OutboxRepository` (`findByOrderByCreatedTimeAsc`, carrying over the source's `PESSIMISTIC_WRITE` + `SKIP LOCKED` query hint unchanged — the standard mechanism for safe concurrent outbox polling across multiple service instances), both re-typed to `UUID` matching Phase 18's domain model. `OrderRepositoryTest` (3 tests) and `OutboxRepositoryTest` (2 tests) written as real `@DataJpaTest`s against an embedded H2 database, not mocks; confirmed the ordering test genuinely catches wrong data by deliberately scrambling the expected order and re-running before reverting. The `SKIP LOCKED` locking guarantee itself isn't independently proven under real concurrent transactions — trusted as documented Hibernate/PostgreSQL-dialect behavior, matching the source's own test scope. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 20 | 2026-08-10 | `order-service` DTOs written: `CreateOrderRequest`, `OrderCreatedEvent`, `RestaurantApprovedEvent`, `RestaurantRejectedEvent` — the repo's first Java records. Id fields typed `UUID` and `OrderCreatedEvent.totalAmount` typed `BigDecimal`, extending Phase 18's typing conventions rather than the source's raw `String`/`double`. The source's `RestaurantTicketStatus` enum was deliberately **not** carried over: grepping confirmed it's defined but never referenced anywhere in the source's `order-service` module, an apparent leftover from its `dto` package being copy-pasted across services without pruning. `./gradlew :order-service:compileJava` and the full 27-test suite both verified green with no regression; no dedicated tests needed for these plain records themselves. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 21 | 2026-08-12 | `order-service` `OrderService` written: `createOrder` (builds a `PENDING` `Order`, saves it, stages a matching `OutboxRecord`, all in one `@Transactional` method), plus `confirmOrder`/`cancelOrder` carrying over the source's double-guard idempotency pattern (cheap `existsByIdAndStatus` check, then `findById`, then a second status re-check after load) with all OpenTelemetry tracing stripped out. New `OrderNotFoundException` replaces the source's generic `IllegalArgumentException`, matching `user-service`'s dedicated-exception-type convention. `createOrder` trusts `CreateOrderRequest.customerId` directly rather than a separate authenticated-identity parameter, since no `api-gateway-service`/auth wiring exists yet to supply one. `OrderServiceTest` (7 tests) written using a real `ObjectMapper` instance rather than a mock, round-tripping the real outbox JSON payload back into `OrderCreatedEvent` to actually prove serialization correctness. Verified with a real `./gradlew :order-service:test` run, and confirmed the suite genuinely catches wrong data by deliberately asserting a wrong `OrderStatus` and re-running before reverting. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 22 | 2026-08-12 | `order-service` `OutboxPublisherService` written: polls `OutboxRepository.findByOrderByCreatedTimeAsc` (Phase 19's `SKIP LOCKED` query) inside one `@Transactional`/`@Scheduled` method, then ships each record's raw JSON payload as-is through `KafkaTemplate<String, String>.send(Message<String>)` — skipping the source's deserialize-then-reserialize round trip through `StreamBridge`/Spring Cloud Stream entirely, since the payload is already the exact wire format needed. Keyed on `aggregateId`, with an `eventType` Kafka header. The send blocks on its `CompletableFuture` so a record is only deleted after a confirmed publish; failures are caught **per record** and logged rather than rolling back the whole batch's transaction like the source's rethrow does, since Phase 21's idempotency guards already tolerate duplicate delivery. `OutboxPublisherServiceTest` (4 tests) covers empty-batch, successful publish+delete, failed-send-leaves-record, and a two-record batch proving one failure doesn't block the other's delete. Verified with a real `./gradlew :order-service:test` run, and confirmed the suite genuinely catches wrong data by deliberately asserting a wrong Kafka key header and re-running before reverting. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 23 | 2026-08-12 | `order-service` `OrderController` written: `POST /orders`, `@Valid @RequestBody CreateOrderRequest` straight into `OrderService.createOrder`, returning the `Order` entity directly (no dedicated response DTO yet — nothing to leak, one caller) with `201 Created`, not the source's `200 OK`. The source's gateway-header (`X-Perimeter-User-Id`) trust-boundary check was deliberately **not** carried over — no `api-gateway-service`/auth wiring exists yet to inject a verified identity, a tracked gap rather than an oversight. `OrderControllerTest` (2 tests, `@WebMvcTest`) covers the happy path and a bean-validation failure. Verified with a real `./gradlew :order-service:test` run, and confirmed the suite genuinely catches wrong data by deliberately asserting a wrong `status` field and re-running before reverting. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 24 | 2026-08-12 | `order-service` `OrderConsumerConfig` written: a plain `@Component` with two `@KafkaListener` methods (`restaurant-approved-topic`/`restaurant-rejected-topic`, groups `order-approved-group`/`order-rejected-group` carried over from the source's naming), each taking the raw JSON `String` payload and deserializing it manually via `ObjectMapper.readValue` — symmetric with how `OutboxPublisherService` publishes — before delegating to `OrderService.confirmOrder`/`cancelOrder`. Replaces the source's Spring Cloud Stream functional `Consumer<Message<T>>` beans and manual OpenTelemetry span-wrapping entirely, neither of which apply to this repo's stack. `OrderConsumerConfigTest` (2 tests, new to this repo — the source has no equivalent) proves real JSON deserializes into the right DTO and dispatches to the right `OrderService` method per topic. Verified with a real `./gradlew :order-service:test` run, confirmed the suite genuinely catches wrong data by deliberately asserting a wrong `reason` field and re-running before reverting, and confirmed adding both `@KafkaListener` consumers plus the `@Scheduled` outbox poller doesn't break `contextLoads()` even with no live Kafka broker running. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 25 | 2026-08-12 | `order-service/application.yaml` written — the module's first, deferred since Phase 16. Follows `user-service`'s `default`/`postgres` profile split (Phase 7) rather than the source's `default`/`k8sdb`, with `order_db`/`order_service_schema` naming. Kafka `bootstrap-servers` defaults to `localhost:9092` (no Minikube infra here), plain `StringSerializer`/`StringDeserializer` on both producer and consumer sides matching `OutboxPublisherService`/`OrderConsumerConfig`'s raw-JSON approach, and `app.outbox.topic`/`batch-size`/`polling-delay-ms` properties reusing the source's own naming/defaults for discoverability. Verified with a real `./gradlew :order-service:test` run that the full module (21 tests, including `contextLoads()`) stays green with no live Kafka broker or Postgres running. This completes the `order-service` service layer + Kafka wiring item tracked since Phase 17 — see the **At a glance** table. Full reasoning in [docs/architecture.md](docs/architecture.md) |

### Local Docker infrastructure

| Phase | Date | What |
| - | - | - |
| 26 | 2026-08-12 | `docker-compose.yml` written: `postgres-db` + `kafka-broker` only, not the source's full Postgres/Kafka/OTel/Prometheus/Loki/Tempo/Grafana stack or its per-service `Dockerfile`s — both explicitly out of scope until a complete multi-service stack exists. Kafka uses a single `PLAINTEXT` listener rather than the source's dual internal/external setup, since no app containers exist yet to need an "internal" side. New `docker/init-schemas.sql` creates `user_service_schema`/`order_service_schema` on first startup via Postgres's `docker-entrypoint-initdb.d` convention. A real, machine-specific port-5432 conflict with an unrelated always-running project (`coolify-full`) was caught *before* any container collision — `postgres-db` remapped to host port 5433, documented in [CONTRIBUTING.md](CONTRIBUTING.md). Verified for real, not assumed: schemas confirmed via `\dn`, Kafka confirmed answering via `kafka-topics --list`, and `order-service` actually booted against both containers (`bootRun`, `postgres` profile) — real `HikariPool`→`PgConnection` connection, and both `@KafkaListener` consumer groups joining and getting real partition assignments against the live broker, not the retry-forever behavior every prior test run saw with no broker present. Full reasoning in [docs/architecture.md](docs/architecture.md) |

### `payment-service` module

| Phase | Date | What |
| - | - | - |
| 27 | 2026-08-12 | `payment-service` scaffold: `settings.gradle.kts` registration, `build.gradle.kts` mirroring `order-service`'s Phase 16 shape (`spring-kafka` direct instead of the source's Cloud Stream binder, Spring Boot 3.5.16 from the start rather than 3.4.1, Lombok pinned to 1.18.42, explicit `springBoot { mainClass.set(...) }`), and `PaymentServiceApplication` entry point. Checking the source's actual `RestaurantConsumerConfig`/`PaymentConsumerConfig` wiring showed `payment-service`, not `restaurant-service`, is the real next link in the saga chain: it consumes `OrderCreatedEvent` (currently published into a void) and publishes `PaymentProcessedEvent`, which `restaurant-service` itself depends on — `restaurant-service` can't be meaningfully built first. `PaymentServiceApplicationTests.contextLoads()` written immediately, before any business logic, per the Phase 17 lesson. No REST controller exists in the source for this module at all; `actuator`+`web` kept anyway for the same HTTP-health-endpoint reasoning as `order-service`, `spring-boot-starter-validation` deliberately not added yet (nothing to validate without a controller). Verified with a real `./gradlew :payment-service:test` run: `contextLoads()` passes with a genuine `HikariPool`/JPA boot in the log. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 28 | 2026-08-12 | `payment-service` domain model written: `Payment` (`UUID` id/`orderId` instead of the source's manually-assigned `String`, `BigDecimal amount` instead of `Double`, `orderId` kept `unique = true` since `PaymentService`'s idempotency check depends on at most one payment per order), `PaymentStatus` (`APPROVED`/`REFUNDED`/`FAILED`, unchanged), `OutboxRecord` (same shape as `order-service`'s Phase 18 version, no `traceId`/`spanId` columns — no tracer to feed them). None of these are new decisions — they're Phase 18's already-established `UUID`/`BigDecimal` conventions applied to a second module. `./gradlew :payment-service:compileJava` and `contextLoads()` both verified green with no regression; no dedicated tests needed for these plain entities themselves. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 29 | 2026-08-12 | `payment-service` repositories written: `PaymentRepository` (`findByOrderId`) and `OutboxRepository` (`SKIP LOCKED` query carried over unchanged from `order-service`'s Phase 19), both re-typed to `UUID` matching Phase 28. `PaymentRepositoryTest` (2 tests: found/not-found by `orderId`) and `OutboxRepositoryTest` (2 tests: empty result, oldest-10-first ordering), both real `@DataJpaTest`s against embedded H2, mirroring `order-service`'s Phase 19 test shape exactly. Verified against real Hibernate DDL (`payments`/`outbox_record` tables genuinely created/dropped per test), and confirmed the ordering test genuinely catches wrong data by deliberately scrambling the expected order and re-running before reverting. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 30 | 2026-08-12 | `payment-service` DTOs and `PaymentService` written. Checking the source's actual code (not just its `dto` package) showed `PaymentFailedEvent` is referenced only in a never-reached dispatch comment and `RestaurantPreparedEvent`/`RestaurantTicketStatus` are referenced nowhere at all — both dropped, the same "don't carry over unreferenced source cruft" call as order-service's Phase 20. The source's local `OrderStatus` copy also had a spurious 4th value (`PAID`) `order-service`'s real enum never produces — corrected to match. `PaymentService.processPaymentSaga`/`handleOrderCompensation` reuse `order-service`'s idempotency-guard shape (Phase 21): `PaymentRepository` gained `existsByOrderId`/`existsByOrderIdAndStatus` for the cheap-check-before-load pattern, and the source's defensive "`OrderCreatedEvent` must be `PENDING`" check was dropped — `OrderService.createOrder` is the only code that ever constructs one, and it's always `PENDING`, making that branch unreachable in this system. New `PaymentNotFoundException` matches `OrderNotFoundException`'s convention. `PaymentServiceTest` (6 tests) uses a real `ObjectMapper`, matching `OrderServiceTest`'s reasoning (Phase 21). Verified with a real `./gradlew :payment-service:test` run, confirmed the suite genuinely catches wrong data by deliberately asserting a wrong `PaymentStatus` and re-running before reverting, and the full 53-test multi-module suite also verified green. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 31 | 2026-08-12 | `payment-service` Kafka wiring completed: `OutboxPublisherService` (ships to `payment-processed-topic` via `KafkaTemplate`), `PaymentConsumerConfig` (`@KafkaListener` on `order-created-topic`/`payment-group` → `processPaymentSaga`, `restaurant-rejected-topic`/`payment-compensation-group` → `handleOrderCompensation`), `application.yaml` (`default`/`postgres` profiles, `payment_db`/`payment_service_schema`) — direct structural repeats of `order-service`'s Phase 22/24/25, no new design forks. `docker/init-schemas.sql` gained a `payment_service_schema` line. `OutboxPublisherServiceTest` (4) and `PaymentConsumerConfigTest` (2, new to this repo like order-service's Phase 24 equivalent) mirror `order-service`'s suites; both confirmed to genuinely catch wrong data via deliberate break-and-revert. Verified end-to-end against real infrastructure, not just mocks: recreated the Docker containers so the updated init script actually ran (it only executes on first container startup), confirmed all 3 schemas via `\dn`, then booted `payment-service` for real (`bootRun`, `postgres` profile) — a genuine `HikariPool`→`PgConnection`, and both consumer groups actually joining and getting real partition assignments against the live broker, the same standard of proof Phase 26 established for `order-service`. This completes `payment-service`'s saga participation: both `order-service` and `payment-service` are now fully wired for their half of the compensating-transaction pair, waiting on `restaurant-service` as the missing middle link. Full reasoning in [docs/architecture.md](docs/architecture.md) |

### `restaurant-service` module

| Phase | Date | What |
| - | - | - |
| 32 | 2026-08-12 | `restaurant-service` scaffold: `settings.gradle.kts` registration, `build.gradle.kts` matching `payment-service`'s Phase 27 shape exactly (`spring-kafka` direct, Boot 3.5.16 from the start, Lombok pinned, explicit `mainClass`, `actuator`+`web` without a controller) — the third repeat of an already-established pattern, no new decisions. `RestaurantServiceApplication` entry point, `RestaurantServiceApplicationTests.contextLoads()` written immediately. Verified with a real `./gradlew :restaurant-service:test` run: passes with a genuine `HikariPool`/JPA boot in the log. Full 60-test multi-module suite also verified green. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 33 | 2026-08-12 | `restaurant-service` domain model written: `RestaurantTicket` (`UUID` id/`orderId` instead of the source's manually-assigned `String` — `order-service`'s `RestaurantApprovedEvent.ticketId` is already `UUID`-typed, so this was effectively already decided by the consuming side), `InventoryItem` (carried over unchanged — `itemCode` `String` primary key is a genuine natural business key, not a surrogate id, so it deliberately doesn't follow the `UUID`-generation convention applied everywhere else), `InventoryStatus`/`RestaurantTicketStatus` (unchanged), `OutboxRecord` (established shape, no trace columns). `RestaurantTicketStatus` is the real, actively-used enum here, distinct from the dead same-named copies already dropped from `order-service`'s (Phase 20) and `payment-service`'s (Phase 30) DTO packages. `./gradlew :restaurant-service:compileJava` and `contextLoads()` both verified green with no regression; no dedicated tests needed for these plain entities. Flagged for the next phase, not decided here: `RestaurantService`'s `PaymentProcessedEvent.status != APPROVED` validation check may be unreachable given `payment-service`'s actual behavior (`PaymentService` only ever publishes with status `APPROVED`), the same shape as the check dropped in Phase 30. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 34 | 2026-08-12 | `restaurant-service` repositories written: `InventoryItemRepository` (no custom query needed — `findById` on the natural-key `itemCode` covers everything), `RestaurantTicketRepository` (`existsByOrderId`, the same idempotency-guard shape as `order-service`/`payment-service`), `OutboxRepository` (`SKIP LOCKED` carried over unchanged) — all re-typed to match Phase 33, no new design decisions. `InventoryItemRepositoryTest` (2), `RestaurantTicketRepositoryTest` (2), `OutboxRepositoryTest` (2) mirror the test shape already established in Phases 19/29. Verified against real Hibernate DDL (`restaurant_inventory`/`restaurant_tickets`/`outbox_record` tables genuinely created/dropped per test), and confirmed the ordering test genuinely catches wrong data by deliberately scrambling the expected order and re-running before reverting. `RestaurantTicketRepository` deliberately has no `findByOrderId` — nothing in `RestaurantService` needs to load and mutate an existing ticket, since `restaurant-service` produces the saga's decision rather than being the target of a later compensating action. Full 66-test multi-module suite also verified green. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 35 | 2026-08-12 | `restaurant-service` DTOs and `RestaurantService` written. All 5 DTOs (`PaymentProcessedEvent`, `PaymentStatus`, `RestaurantApprovedEvent`, `RestaurantRejectedEvent`, sealed `RestaurantEvent` interface) are genuinely used — no dead code to drop this time, unlike order-service's/payment-service's DTO phases. `RestaurantService.processRestaurantStep` keeps the source's null-field and `quantity`/`amount` validation checks (real defense against a malformed message on shared Kafka infrastructure, not an intra-repo unreachable branch) but drops `status != APPROVED`, matching Phase 30's precedent exactly — `PaymentService` never publishes a `PaymentProcessedEvent` except with status `APPROVED`. `RestaurantServiceTest` (6 tests) and a new `RestaurantInventoryServiceTest` (3 tests, no source equivalent — covers real stock-deduction branching `RestaurantServiceTest`'s mocked inventory service can't exercise) verified with a real `./gradlew :restaurant-service:test` run; both confirmed to genuinely catch wrong data via deliberate break-and-revert. Full 75-test multi-module suite also verified green. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 36 | 2026-08-12 | `restaurant-service` Kafka wiring completed: `OutboxPublisherService` routes to one of two topics (`restaurant-approved-topic`/`restaurant-rejected-topic`) by switching on `OutboxRecord.getEventType()` — a plain string column, not deserialization — then ships the raw payload as-is, avoiding the deserialize-then-reserialize round trip the source's typed-switch approach reintroduces; the one real design fork from `order-service`/`payment-service`'s single-fixed-topic shape. An unrecognized `eventType` throws, a real guard since nothing in this class controls what lands in the table. `RestaurantConsumerConfig` (`@KafkaListener` on `payment-processed-topic`/`restaurant-group` → `processRestaurantStep`), `application.yaml` (`default`/`postgres` profiles, `restaurant_db`/`restaurant_service_schema`) — otherwise the same shape as `order-service`/`payment-service`'s Phase 22/24/25. `docker/init-schemas.sql` gained a `restaurant_service_schema` line. `OutboxPublisherServiceTest` (5, including real two-topic-routing and unknown-eventType coverage the single-topic modules never needed) and `RestaurantConsumerConfigTest` (1) verified with a real `./gradlew :restaurant-service:test` run (22 tests in this module), both confirmed to genuinely catch wrong data via deliberate break-and-revert. Verified end-to-end against real infrastructure: recreated Docker containers, confirmed all 4 service schemas via `\dn`, booted `restaurant-service` for real (`bootRun`, `postgres` profile) — genuine `HikariPool`→`PgConnection`, and `restaurant-group` actually joining and getting a real partition assignment against the live broker. **This closes the full saga chain end-to-end**: `order-service` → `payment-service` → `restaurant-service`, with both `order-service` and `payment-service` reacting to `restaurant-service`'s decision. `api-gateway-service` is the only unbuilt module from the original four-service plan. Full reasoning in [docs/architecture.md](docs/architecture.md) |

### `api-gateway-service` module

| Phase | Date | What |
| - | - | - |
| 37 | 2026-08-13 | `api-gateway-service` scaffold: `settings.gradle.kts` registration, `build.gradle.kts` — this repo's first reactive stack (Spring WebFlux/Netty + Spring Cloud Gateway's `webflux` server variant, needed because a gateway proxies many concurrent downstream calls without tying up a thread per request), staying on Boot 3.5.16 rather than the source's 4.0.6, paired with the matching Spring Cloud **2025.0.0** BOM and the same spring-grpc **0.7.0** milestone `user-service` already uses. Resolved and compiled clean on the first attempt, no version conflicts. OTel (`TracingConfig`, `ObservationConfig`, OTLP config) and the `k8sdb` Kubernetes-DNS profile both dropped, matching every other module's established local-dev-only scope; no `Dockerfile` yet either, matching `order-service`/`payment-service`/`restaurant-service`. `ApiGatewayServiceApplication` entry point. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 38 | 2026-08-13 | `api-gateway-service` gRPC auth client: `CommonAppConfig` wires a `UserIdentityServiceBlockingStub` against this repo's real `Login` RPC (not the source's `authenticateUser`/`AuthResponse` shape, which belongs to its own different `user-contract`). `UserGrpcClient.login()`, `AuthRequest` (field renamed `email`, not the source's misleading `username`), `WebTokenResponse`, and the four gateway-local exception types. `UserGrpcExceptionTranslator`'s gRPC-status-to-exception mapping confirmed correct against `user-service`'s own `GrpcExecutor` (Phase 7), not just copied on faith. `AuthenticationController` (`POST /auth/login`) and `GlobalExceptionHandler` (exception-to-HTTP-status table, minus the OTel span-enrichment code dropped in Phase 37). `./gradlew :api-gateway-service:compileJava` verified green. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 39 | 2026-08-13 | `JwtPerimeterGuardGatewayFilterFactory` written — a direct, unmodified port verifying tokens against the same `app.jwt.secret`/`issuer` `user-service`'s `JwtTokenProvider` signs with, extracting the `user-id` claim into an `X-Perimeter-User-Id` downstream header. `GatewayFallbackController` and `application.yaml`'s route table (`auth-token-route`, `order-service-route` guarded by `JwtPerimeterGuard` + a Resilience4j `CircuitBreaker` falling back to `/fallback/orders`), all pointed at `localhost` rather than Kubernetes DNS. `ApiGatewayServiceApplicationTests.contextLoads()` written and run for real: passed on the first try, with genuine log evidence the filter, full Spring Cloud Gateway route-predicate set, and gRPC stub bean all initialized correctly — no live `order-service`/`user-service` needed. Full reasoning in [docs/architecture.md](docs/architecture.md) |
| 40 | 2026-08-13 | `api-gateway-service` tests completed: `AuthenticationControllerTest` (3, `@WebFluxTest`). Porting `JwtPerimeterGuardGatewayFilterFactoryTest` and actually running it (never assumed) surfaced a **real bug in the source**: two of its three tests asserted an HTTP response status the filter never sets — only `GlobalExceptionHandler` sets one, a layer absent from an isolated filter unit test — and failed immediately with `onError` instead of the expected `onComplete`. Rewrote both to assert the filter's real, provable contract instead (`StepVerifier.expectError(JWTVerificationException.class)` + `verifyNoInteractions(filterChain)`); also corrected a mislabeled "Forbidden" (403) expectation for a tampered token, which `GlobalExceptionHandler` actually maps to 401 (403 is `TokenExpiredException`-only). Also dropped the source test's unnecessary Guava `HttpHeaders` import for Spring's own. Verified with a real `./gradlew :api-gateway-service:test` run (7 tests, all passing), confirmed the suite genuinely catches wrong data via deliberate break-and-revert on the happy-path token assertion, and confirmed the full multi-module suite (88 tests across all 6 modules) stays green. **Completes all five originally-planned backend modules.** Full reasoning in [docs/architecture.md](docs/architecture.md) |

### Security fixes

| Phase | Date | What |
| - | - | - |
| 41 | 2026-08-13 | GitHub CodeQL flagged a real Medium-severity log-injection finding (CWE-117) in `OrderController.createOrder` (Phase 23): `log.info("...: {}", request)` interpolated the whole `CreateOrderRequest` record, and `itemCode` — a `@NotBlank String` with no length cap or character restriction, taken straight off the public `POST /orders` body — flows into that `toString()` unsanitized, letting a client forge fake log lines with embedded `\r`/`\n`. Checked every other `log.info`/`log.warn` call site across all five modules for the same pattern: everywhere else already logs individual, type-safe fields, confirming this was the one call site that slipped through, not a repo-wide issue. Fixed by logging `customerId`/`itemCode`/`quantity`/`totalAmount` individually, with `itemCode` passed through `.replaceAll("[\r\n]", "_")` — the only field with actual string-injection surface, since the other three are `UUID`/`BigDecimal`/`int`. `./gradlew :order-service:test` verified green with no regressions. Full reasoning in [docs/architecture.md](docs/architecture.md) |

### Repo hygiene

| Phase | Date | What |
| - | - | - |
| 42 | 2026-08-28 | `cleanLogs`, a new root `build.gradle.kts` task, removes every untracked `.log` file in the repo — found via `git ls-files` rather than a hand-maintained list of directories/filenames to skip, so it respects `.gitignore` automatically (this repo's own `.gitignore` has a blanket `*.log` rule, so `--exclude-standard` alone would always return nothing here — unioned with an `--ignored` pass to still find them) and stays correct for any future `.log` file with zero changes needed. No `node_modules`/`vendor`-style exclusion needed, unlike the equivalent scripts added the same day to the conduit-full/coolify-full sibling repos — Gradle's dependency cache lives entirely outside this repo, in `~/.gradle`. Wired as a `dependsOn` for every subproject's `bootRun` task (matched by task name, not the Spring Boot plugin's `BootRun` class, since that plugin is applied per-service, not at the root), so it runs automatically before the actual per-service startup command this repo documents in [CONTRIBUTING.md](CONTRIBUTING.md). Verified for real, not just by inspection: `./gradlew cleanLogs` correctly found and removed both root-level and nested `.log` files and reported "No log files found" on a clean tree; `--dry-run` on all 5 real services' `bootRun` confirmed `:cleanLogs` runs first in each task graph every time |
| 43 | 2026-09-06 | CI speed ([#47](https://github.com/Terrence721/saga-full/issues/47)) applied: root `gradle.properties` with `org.gradle.caching=true` + `org.gradle.parallel=true`. Verified with real repeated `./gradlew clean` + `./gradlew test --continue` runs — build-cache reuse confirmed (17-19 of 30 tasks restored `FROM-CACHE`/`UP-TO-DATE` on a clean tree once only the touched files actually changed) and wall-clock dropped from ~1m56s to as low as 6s on a fully-cached run. Real finding surfaced by enabling parallel module execution: `AuthenticationControllerTest` genuinely failed once with `IllegalStateException: Timeout on blocking read for 5000000000 NANOSECONDS` — WebTestClient's default 5s response timeout, tripped by CPU contention from 6 modules' test JVMs running concurrently (reproduced on an 8-core machine; GitHub's hosted runners have fewer cores, so CI was equally or more exposed). Not a code regression — the same suite passed standalone immediately after. Fixed at the root cause, not by capping parallelism: added `@AutoConfigureWebTestClient(timeout = "PT15S")` to all 4 test classes repo-wide that rely on the default-configured `WebTestClient` (`AuthenticationControllerTest`, `AuthenticationControllerThreadingTest`, `GatewayFallbackControllerTest`, `JwtPerimeterGuardIntegrationTest`), since all 4 share the identical latent fragility, not just the one that happened to flake this run. Two full clean+test runs afterward both passed with no timeout. Full repo suite green, 157/157 (unchanged — annotation-only test changes) |

### Docker / deployment

| Phase | Date | What |
| - | - | - |
| 44 | 2026-09-06 | Per-service `Dockerfile`s ([#14](https://github.com/Terrence721/saga-full/issues/14)): multi-stage builds (`eclipse-temurin:25-jdk` to build, `eclipse-temurin:25-jre` to run, non-root user) for all 5 services. Each builds with the repo root as context, not its own directory — verified empirically that `settings.gradle.kts`'s `include()` of all 6 modules means Gradle refuses to configure the build unless every included module's directory physically exists, even for a module with zero actual dependency on the others (confirmed: `:order-service:bootJar` alone, without the other 5 module directories present, fails with "Configuring project ':user-contract' without an existing directory is not allowed"). `docker-compose.yml` extended with all 5 services, wired to the existing Postgres/Kafka containers via the `postgres` Spring profile and `KAFKA_BOOTSTRAP_SERVERS`. Real, necessary fix along the way: `kafka-broker`'s single `PLAINTEXT://localhost:9092` advertised listener only worked for host-based connections (the existing non-Docker `bootRun` workflow) — a container connecting via `kafka-broker:9092` would be told, after its initial bootstrap handshake, to reconnect to `localhost:9092`, which inside that container's own network namespace doesn't mean the broker at all. Fixed with the standard dual-listener pattern: a `PLAINTEXT_HOST` listener (unchanged, for the host) plus a new `PLAINTEXT_INTERNAL` listener on `kafka-broker:29092` (for containers). Verified end-to-end, not assumed: brought up the full 7-container stack for real, inserted a test user directly via `psql`, logged in through the containerized gateway, and created a real order — watched it flow through the complete distributed saga across containers, **including the compensation path** (an unseeded item code triggered a real restaurant rejection, which correctly triggered both a payment refund and an order cancellation, proving the rollback direction works too, not just the happy path). One more real bug found this way: the gateway's Resilience4j circuit breaker had no explicit `TimeLimiter` timeout configured, silently defaulting to the library's 1-second default — too tight for a cold cross-container HTTP call (DNS + connection-pool warmup on the Docker bridge network gave a real, measured 1.29s first call, which the old default would reject but the code already worked). Fixed by adding an explicit `resilience4j.timelimiter.instances.orderServiceCircuitBreaker.timeout-duration: 5s`, then re-verified from a completely fresh stack restart that the very first cold order call succeeds. Also added a root `.dockerignore` and a "Running the full stack" section to [CONTRIBUTING.md](CONTRIBUTING.md). Kubernetes manifests remain out of scope for this issue (see **Still to do**) |

### Register/POS frontend (backend groundwork) + architectural audit

| Phase | Date | What |
| - | - | - |
| 45 | 2026-09-06 | `order-service` gained `GET /orders/{id}` ([#199](https://github.com/Terrence721/saga-full/issues/199)/[PR #200](https://github.com/Terrence721/saga-full/pull/200)) — step 1 of the frontend plan (#15). `OrderService.getOrder(UUID)` reuses the existing private `findOrder` helper; `OrderController.getOrder` enforces the same `X-Perimeter-User-Id === order.customerId` fail-closed invariant `createOrder` already enforces, and a new `OrderController.handleOrderNotFound` gives the module its first HTTP-facing exception handler (`OrderNotFoundException` previously only ever propagated through the Kafka-consumer path). 4 new `OrderControllerTest` cases; verified via deliberate revert that the ownership check genuinely gates access |
| 46 | 2026-09-09 | `order-service` gained a live SSE stream, `GET /orders/{id}/stream` ([#201](https://github.com/Terrence721/saga-full/issues/201)/[PR #202](https://github.com/Terrence721/saga-full/pull/202)) — step 2. A shared `Sinks.Many<Order>` on `OrderService`, emitted right after each of `createOrder`/`confirmOrder`/`cancelOrder`'s existing `save()` calls; `OrderController.streamOrder` concatenates `getOrder(id)`'s current-state snapshot with the live filtered stream, returning `Flux<ServerSentEvent<Order>>` over a normal Spring MVC servlet response (`reactor-core`/`reactor-test` added — MVC's `ReactiveTypeHandler` handles this once reactor-core is on the classpath, no WebFlux needed). 8 new tests across `OrderServiceTest`/`OrderControllerTest` |
| 47 | 2026-09-09 | **Real bug found and fixed** ([#203](https://github.com/Terrence721/saga-full/issues/203)/[PR #204](https://github.com/Terrence721/saga-full/pull/204)): live end-to-end verification of Phase 46's stream (not caught by any unit test — every test there subscribes before the emission happens) showed a late-connecting client replaying stale `PENDING`→`SUCCESS` history after its own current-state snapshot already showed `SUCCESS`. Root cause: `Sinks.many().multicast().onBackpressureBuffer()` queues *any* emission made while zero subscribers are connected and replays the full backlog to the next one that arrives, regardless of relevance. Fixed by switching to `Sinks.many().multicast().directBestEffort()`, which only delivers to subscribers connected at the exact moment of emission — the semantics the field's own doc comment already claimed but the wrong sink type didn't enforce. No test changes needed (every existing `StepVerifier` test subscribes before emitting, so it passes identically under both sink types); re-verified against the real stack |
| 48 | 2026-09-09 | `api-gateway-service` gained the routing + CORS the frontend needs ([#205](https://github.com/Terrence721/saga-full/issues/205)/[PR #206](https://github.com/Terrence721/saga-full/pull/206)) — step 3: two new `JwtPerimeterGuard`-protected routes (`order-get-route`, `order-stream-route`), plus `globalcors` YAML config (superseded in Phase 52 below). **Real bug found and fixed**: Spring Cloud Gateway Server WebFlux 4.3.0's `MethodRoutePredicateFactory` matches a request's own HTTP method with zero preflight-awareness, so a `POST`/`GET`-only route never matches a browser's `OPTIONS` preflight at all — confirmed via bytecode inspection of `AbstractHandlerMapping.getHandler()`, whose CORS-processing step only runs once a route has already matched. Fixed by adding `OPTIONS` to every guarded route's `Method` predicate list — safe, since Spring's own handler mapping short-circuits a matched preflight before any route filter (`JwtPerimeterGuard` included) runs. 5 new tests (`GlobalCorsConfigTest` ×3, `JwtPerimeterGuardIntegrationTest` ×2); verified via deliberate revert |
| 49 | 2026-09-09 | **Architectural DRY/SOLID/composition audit** ([#207](https://github.com/Terrence721/saga-full/issues/207)/[PR #208](https://github.com/Terrence721/saga-full/pull/208)) — a read-only scan, a separate lens from the correctness/security/test-coverage audit (#17). Found and fixed: (1) `OrderService`/`PaymentService`/`RestaurantService` each hand-rolled an identical serialize-and-build-outbox-record sequence with a direct `ObjectMapper` dependency — extracted a per-module `OutboxRecordFactory` component owning that policy, so each domain service now only does its own entity→event-DTO mapping; (2) `order-service`'s `OrderService` also owned Phase 46's `Sinks.Many<Order>` push mechanism, an unrelated concern — extracted into a standalone `OrderUpdatePublisher` that `OrderController` now depends on directly for streaming; (3) each module's `*ConsumerConfig` class bundled Kafka-listener routing with its `DefaultErrorHandler` retry-policy bean — split into a per-module `KafkaErrorHandlerConfig`. Reviewed and deliberately left alone: `validateCustomerMatches`'s duplication between `order-service`/`payment-service` (different domain types, 5 lines, used once each — the abstraction would cost more than the duplication), and the byte-for-byte `OutboxPublisherService`/`OutboxRecord`/`OutboxRepository` repetition across all three services (an already-accepted tradeoff from the original audit). 7 new files, each with its own dedicated tests moved/adapted from the classes they were extracted from; every delegation point verified via deliberate revert. 0 findings left open |
| 50 | 2026-09-09 | Frontend shell scaffolded ([#209](https://github.com/Terrence721/saga-full/issues/209)/[PR #210](https://github.com/Terrence721/saga-full/pull/210)) — step 4: new top-level `frontend/` directory, Vite + React 19 + TypeScript 6 (`strict: true` added to both tsconfig files — the template ships without it), Yarn 4.18.0 with the `node-modules` linker (not npm, not PnP) pinned via `packageManager` in `package.json`, Tailwind CSS 4 wired through `@tailwindcss/vite` and confirmed rendering live in a browser. Dev server pinned to port 5180 (`strictPort: true`) after finding a real cross-project collision — `coolify-full`'s own Vite dev server already claims the default 5173 on this machine — with `api-gateway-service`'s `FRONTEND_ORIGIN` CORS default updated to match. Deliberately deferred, not silently dropped: `frontend/README.md` and `frontend/public/favicon.svg` are still generic `create-vite` template content, to be revisited once there's a real UI/brand to describe |
| 51 | 2026-09-09 | Frontend login flow built and wired in ([#211](https://github.com/Terrence721/saga-full/issues/211)/[#213](https://github.com/Terrence721/saga-full/issues/213)/[#215](https://github.com/Terrence721/saga-full/issues/215), PRs [#212](https://github.com/Terrence721/saga-full/pull/212)/[#214](https://github.com/Terrence721/saga-full/pull/214)/[#216](https://github.com/Terrence721/saga-full/pull/216)/[#217](https://github.com/Terrence721/saga-full/pull/217)/[#218](https://github.com/Terrence721/saga-full/pull/218)) — step 5: `src/api/httpClient.ts` (shared `apiFetch<T>()` + typed `ApiError`, extracted proactively so a later `orderClient.ts` won't re-triplicate the same fetch/error-normalization logic Phase 49 just finished de-duplicating on the backend), `src/api/authClient.ts` (`login()` against the real `POST /auth/login`), a 3-way-split `AuthContext`/`AuthProvider`/`useAuth` (oxlint's `react-refresh/only-export-components` rejects a single file mixing component + hook + context exports), `LoginForm`, and `App.tsx` wiring (conditional render: login screen until authenticated). JWT kept in React state, not `localStorage` — avoids the XSS-token-theft surface. **Real bug found and fixed**: `AuthContext.ts`/`AuthContext.tsx` (pre-split names) differed only in casing — built fine individually on this case-insensitive Windows filesystem, but broke with TypeScript's "differs...only in casing" error (TS1149/TS1261) once `App.tsx`'s import graph pulled both into the same program; fixed by renaming to genuinely distinct names (`AuthContext.ts`, `AuthProvider.tsx`) via a sequenced `git mv` that never passed through an intermediate collision. Issues #211 and #213 both closed prematurely when their respective PRs merged with only partial scope — the process was changed afterward to use "Part of #N" rather than "Closes #N" in PR bodies for incremental work, so the tracking issue stays open until its full scope actually lands |
| 52 | 2026-09-09 | **Real bug found and fixed**, only reachable once real browser verification of Phase 51's login flow was possible ([PR #219](https://github.com/Terrence721/saga-full/pull/219)): `/auth/login`'s real preflight got rejected with 403 even though Phase 48's CORS config looked correct on paper. Root cause: `AuthenticationController` (a real local `@RestController`) and the gateway's own `auth-token-route` both map `/auth/login`, and Spring's `RequestMappingHandlerMapping` (standard controller dispatch) claims the path before `RoutePredicateHandlerMapping` (the gateway's own routing) ever sees it — `globalcors` only wires into the latter, so the request that actually gets served has no CORS configuration behind it at all. Diagnosed empirically with a temporary diagnostic `WebFilter` inspecting `ServerWebExchangeUtils.GATEWAY_ROUTE_ATTR` (`null` on the broken path, populated on a working gateway-routed one) before writing any fix. Fixed with `GlobalCorsConfig`, a `HandlerMapping`-agnostic `CorsWebFilter` bean that applies uniformly regardless of which mapping serves a request — replacing the `globalcors` YAML block entirely rather than running alongside it. The same PR fixed a real Dockerfile inefficiency found while iterating on this bug: none of the 5 service `Dockerfile`s used a BuildKit cache mount for `~/.gradle`, so every rebuild that touched source re-downloaded the Gradle distribution and re-resolved every dependency from Maven Central from scratch (10+ minutes observed) — fixed by adding `--mount=type=cache,target=/root/.gradle` to each `RUN ./gradlew :<module>:bootJar` line. New `GlobalCorsConfigTest` regression case verified via deliberate revert; login confirmed working end-to-end against the real running gateway in a real browser. **181/181 tests passing, repo-wide** |

## 🔍 Code review — per-file tracking

Process detail: [docs/code-review.md](docs/code-review.md). Parent tracking issue [#17](https://github.com/Terrence721/saga-full/issues/17), one sub-issue per module ([#18](https://github.com/Terrence721/saga-full/issues/18) `user-contract` – [#23](https://github.com/Terrence721/saga-full/issues/23) `api-gateway-service`), same Backlog/Planned/In Progress/Verification & QA/Done board as the rest of this repo's work. One module worked at a time, same dependency order the modules were originally built in; this section holds the table for whichever module is currently in progress, and is replaced by the next module's table once every row here is closed out.

**Total scope:** 73 files across 6 modules (`user-contract` 1, `user-service` 10, `order-service` 15, `payment-service` 14, `restaurant-service` 18, `api-gateway-service` 15).

**`user-contract` module review complete — 1/1 files reviewed, 0 real bugs found, 1 structural note recorded** (`ValidateToken` has no real caller anywhere in this repo yet, contradicting `docs/architecture.md`'s own Phase 7 plan — flagged for a decision, not fixed). See [docs/code-review.md](docs/code-review.md) for the full write-up and [#18](https://github.com/Terrence721/saga-full/issues/18) for the closed tracking issue.

### `user-service` (module tracking issue [#19](https://github.com/Terrence721/saga-full/issues/19)) — started 2026-08-13

| File | Last commit SHA | Sub-issue | PR | Status |
| - | - | - | - | - |
| `src/main/java/.../UserServiceApplication.java` | [`43cddfe`](https://github.com/Terrence721/saga-full/commit/43cddfe) | [#26](https://github.com/Terrence721/saga-full/issues/26) | [#27](https://github.com/Terrence721/saga-full/pull/27) | Done — no findings, merged |
| `src/main/java/.../config/SecurityConfig.java` | [`43cddfe`](https://github.com/Terrence721/saga-full/commit/43cddfe) | [#28](https://github.com/Terrence721/saga-full/issues/28) | [#29](https://github.com/Terrence721/saga-full/pull/29) | Done — real test-coverage gap fixed (`encode()` never exercised anywhere in the repo), merged |
| `src/main/java/.../domain/User.java` | [`43cddfe`](https://github.com/Terrence721/saga-full/commit/43cddfe) | [#30](https://github.com/Terrence721/saga-full/issues/30) | [#31](https://github.com/Terrence721/saga-full/pull/31) | Done — no findings, merged |
| `src/main/java/.../exception/InvalidCredentialsException.java` | [`43cddfe`](https://github.com/Terrence721/saga-full/commit/43cddfe) | [#32](https://github.com/Terrence721/saga-full/issues/32) | [#33](https://github.com/Terrence721/saga-full/pull/33) | Done — no findings, merged |
| `src/main/java/.../exception/UserInactiveException.java` | [`43cddfe`](https://github.com/Terrence721/saga-full/commit/43cddfe) | [#34](https://github.com/Terrence721/saga-full/issues/34) | [#35](https://github.com/Terrence721/saga-full/pull/35) | Done — no findings, merged |
| `src/main/java/.../exception/UserNotFoundException.java` | [`43cddfe`](https://github.com/Terrence721/saga-full/commit/43cddfe) | [#36](https://github.com/Terrence721/saga-full/issues/36) | [#37](https://github.com/Terrence721/saga-full/pull/37) | Done — no findings, merged |
| `src/main/java/.../infra/grpc/GrpcExecutor.java` | [`43cddfe`](https://github.com/Terrence721/saga-full/commit/43cddfe) | [#38](https://github.com/Terrence721/saga-full/issues/38) | [#39](https://github.com/Terrence721/saga-full/pull/39) | Done — real security fix (login enumeration, CWE-203, collapsed to generic UNAUTHENTICATED), merged |
| `src/main/java/.../infra/grpc/UserGrpcServiceImpl.java` | [`43cddfe`](https://github.com/Terrence721/saga-full/commit/43cddfe) | [#40](https://github.com/Terrence721/saga-full/issues/40) | [#41](https://github.com/Terrence721/saga-full/pull/41) | Done — real security fix (login timing side-channel, CWE-208, constant-cost password check), merged |
| `src/main/java/.../infra/security/JwtTokenProvider.java` | [`43cddfe`](https://github.com/Terrence721/saga-full/commit/43cddfe) | [#42](https://github.com/Terrence721/saga-full/issues/42) | [#43](https://github.com/Terrence721/saga-full/pull/43) | Done — minor fix (cache JWTVerifier once, matching api-gateway-service's pattern), merged |
| `src/main/java/.../repository/UserRepository.java` | [`43cddfe`](https://github.com/Terrence721/saga-full/commit/43cddfe) | [#44](https://github.com/Terrence721/saga-full/issues/44) | [#45](https://github.com/Terrence721/saga-full/pull/45) | Done — real test-coverage gap fixed (no `@DataJpaTest` existed for this module), merged |

**`user-service` module review complete — 10/10 files reviewed, 5 real findings fixed** (a test-coverage gap in `SecurityConfig.java`, a login-enumeration security fix in `GrpcExecutor.java`, its timing-side-channel sibling in `UserGrpcServiceImpl.java`, a minor efficiency fix in `JwtTokenProvider.java`, and a test-coverage gap in `UserRepository.java`), plus 2 structural notes flagged for later (unused `ValidateToken` RPC, case-sensitive email lookups — both blocked on a registration flow that doesn't exist yet). See [docs/code-review.md](docs/code-review.md) for the full write-up and [#19](https://github.com/Terrence721/saga-full/issues/19) for the closed tracking issue.

**`order-service` module review complete — 15/15 files reviewed, 5 issues fixed, 0 findings left open** (an IDOR fix and 2 CodeQL log-injection follow-ups in `OrderController.java`, a DTO-validation fix in `CreateOrderRequest.java`, a Kafka poison-pill gap fixed in `OrderConsumerConfig.java`, a CWE-117 log-injection fix plus `customerId`/`ticketId` validation in `OrderService.java`, and an unbounded-Kafka-timeout fix in `OutboxPublisherService.java`). Every finding deferred during this module's review landed as a real fix by the time the module closed, including a correction to an earlier write-up's inaccurate claim once verified against Spring Kafka's real default behavior. Full multi-module suite: 107/107 tests passing. See [docs/code-review.md](docs/code-review.md) for the full write-up and [#20](https://github.com/Terrence721/saga-full/issues/20) for the closed tracking issue.

### `payment-service` (module tracking issue [#21](https://github.com/Terrence721/saga-full/issues/21)) — started 2026-08-19

| File | Last commit SHA | Sub-issue | PR | Status |
| - | - | - | - | - |
| `src/main/java/.../PaymentServiceApplication.java` | [`3f9e7c9`](https://github.com/Terrence721/saga-full/commit/3f9e7c9) | [#82](https://github.com/Terrence721/saga-full/issues/82) | [#83](https://github.com/Terrence721/saga-full/pull/83) | Done — no findings, merged |
| `src/main/java/.../domain/OutboxRecord.java` | [`f9235f7`](https://github.com/Terrence721/saga-full/commit/f9235f7) | [#84](https://github.com/Terrence721/saga-full/issues/84) | [#85](https://github.com/Terrence721/saga-full/pull/85) | Done — no findings, merged |
| `src/main/java/.../domain/Payment.java` | [`f9235f7`](https://github.com/Terrence721/saga-full/commit/f9235f7) | [#86](https://github.com/Terrence721/saga-full/issues/86) | [#87](https://github.com/Terrence721/saga-full/pull/87) | Done — no findings, merged (later extended with a `customerId` column under #96/PR #97 — see `RestaurantRejectedEvent.java`'s row) |
| `src/main/java/.../domain/PaymentStatus.java` | [`f9235f7`](https://github.com/Terrence721/saga-full/commit/f9235f7) | [#88](https://github.com/Terrence721/saga-full/issues/88) | [#89](https://github.com/Terrence721/saga-full/pull/89) | Done — real finding fixed (payment decline path added), merged |
| `src/main/java/.../dto/OrderCreatedEvent.java` | [`86f5847`](https://github.com/Terrence721/saga-full/commit/86f5847) | [#90](https://github.com/Terrence721/saga-full/issues/90) | [#91](https://github.com/Terrence721/saga-full/pull/91) | Done — no findings, merged |
| `src/main/java/.../dto/OrderStatus.java` | [`86f5847`](https://github.com/Terrence721/saga-full/commit/86f5847) | [#92](https://github.com/Terrence721/saga-full/issues/92) | [#93](https://github.com/Terrence721/saga-full/pull/93) | Done — no findings, merged |
| `src/main/java/.../dto/PaymentProcessedEvent.java` | [`86f5847`](https://github.com/Terrence721/saga-full/commit/86f5847) | [#94](https://github.com/Terrence721/saga-full/issues/94) | [#95](https://github.com/Terrence721/saga-full/pull/95) | Done — no findings, merged |
| `src/main/java/.../dto/RestaurantRejectedEvent.java` | [`86f5847`](https://github.com/Terrence721/saga-full/commit/86f5847) | [#96](https://github.com/Terrence721/saga-full/issues/96) | [#97](https://github.com/Terrence721/saga-full/pull/97) | Done — real finding fixed (customerId cross-check added), merged |
| `src/main/java/.../exception/PaymentNotFoundException.java` | [`86f5847`](https://github.com/Terrence721/saga-full/commit/86f5847) | [#98](https://github.com/Terrence721/saga-full/issues/98) | [#99](https://github.com/Terrence721/saga-full/pull/99) | Done — structural note recorded, not fixed here, merged |
| `src/main/java/.../repository/OutboxRepository.java` | [`382063d`](https://github.com/Terrence721/saga-full/commit/382063d) | [#100](https://github.com/Terrence721/saga-full/issues/100) | [#101](https://github.com/Terrence721/saga-full/pull/101) | Done — no findings, merged |
| `src/main/java/.../repository/PaymentRepository.java` | [`86f5847`](https://github.com/Terrence721/saga-full/commit/86f5847) | [#102](https://github.com/Terrence721/saga-full/issues/102) | [#103](https://github.com/Terrence721/saga-full/pull/103) | Done — real finding fixed (test-coverage gap: existsByOrderId/existsByOrderIdAndStatus had no @DataJpaTest), merged |
| `src/main/java/.../service/OutboxPublisherService.java` | [`e585caf`](https://github.com/Terrence721/saga-full/commit/e585caf) | [#104](https://github.com/Terrence721/saga-full/issues/104) | [#105](https://github.com/Terrence721/saga-full/pull/105) | Done — known finding fixed (unbounded Kafka-send timeout, same as order-service #80), merged |
| `src/main/java/.../service/PaymentConsumerConfig.java` | [`e585caf`](https://github.com/Terrence721/saga-full/commit/e585caf) | [#106](https://github.com/Terrence721/saga-full/issues/106) | [#107](https://github.com/Terrence721/saga-full/pull/107) | Done — known finding fixed (missing DefaultErrorHandler, same as order-service #76), merged |
| `src/main/java/.../service/PaymentService.java` | [`86f5847`](https://github.com/Terrence721/saga-full/commit/86f5847) | [#88](https://github.com/Terrence721/saga-full/issues/88) | [#89](https://github.com/Terrence721/saga-full/pull/89) | Done — covered by `PaymentStatus.java`'s review (#88), decline path + compensation guard, merged (further extended with a customerId cross-check under #96/PR #97) |

**`payment-service` module review complete — 14/14 files reviewed, 4 real findings fixed** (a payment-decline path that never existed in `PaymentStatus.java`, a refund compensation that never checked whose order it was refunding in `RestaurantRejectedEvent.java`, a test-coverage gap on two idempotency-guard queries in `PaymentRepository.java`, and an unbounded Kafka-send timeout in `OutboxPublisherService.java`), plus a known-finding fix carried over from order-service's precedent in `PaymentConsumerConfig.java` (missing `DefaultErrorHandler`), 0 findings left open. `PaymentStatus.java`'s review also cascaded a fix into `restaurant-service/RestaurantService.java` ahead of that module's own audit turn. See [docs/code-review.md](docs/code-review.md) for the full write-up and [#21](https://github.com/Terrence721/saga-full/issues/21) for the closed tracking issue.

### `restaurant-service` (module tracking issue [#22](https://github.com/Terrence721/saga-full/issues/22)) — started 2026-08-25

| File | Last commit SHA | Sub-issue | PR | Status |
| - | - | - | - | - |
| `src/main/java/.../RestaurantServiceApplication.java` | [`348f7ba`](https://github.com/Terrence721/saga-full/commit/348f7ba) | [#108](https://github.com/Terrence721/saga-full/issues/108) | [#109](https://github.com/Terrence721/saga-full/pull/109) | Done — no findings, merged |
| `src/main/java/.../domain/InventoryItem.java` | [`c887140`](https://github.com/Terrence721/saga-full/commit/c887140) | [#110](https://github.com/Terrence721/saga-full/issues/110) | [#111](https://github.com/Terrence721/saga-full/pull/111) | Done — real finding fixed (no way to ever seed inventory in a real deployment — added data.sql), merged |
| `src/main/java/.../domain/InventoryStatus.java` | [`c887140`](https://github.com/Terrence721/saga-full/commit/c887140) | [#112](https://github.com/Terrence721/saga-full/issues/112) | [#113](https://github.com/Terrence721/saga-full/pull/113) | Done — no findings, merged |
| `src/main/java/.../domain/OutboxRecord.java` | [`c887140`](https://github.com/Terrence721/saga-full/commit/c887140) | [#114](https://github.com/Terrence721/saga-full/issues/114) | [#115](https://github.com/Terrence721/saga-full/pull/115) | Done — no findings, merged |
| `src/main/java/.../domain/RestaurantTicket.java` | [`c887140`](https://github.com/Terrence721/saga-full/commit/c887140) | [#116](https://github.com/Terrence721/saga-full/issues/116) | [#117](https://github.com/Terrence721/saga-full/pull/117) | Done — real finding fixed (missing order_id unique constraint), merged |
| `src/main/java/.../domain/RestaurantTicketStatus.java` | [`c887140`](https://github.com/Terrence721/saga-full/commit/c887140) | [#123](https://github.com/Terrence721/saga-full/issues/123) | [#124](https://github.com/Terrence721/saga-full/pull/124) | Done — no findings, merged |
| `src/main/java/.../dto/PaymentProcessedEvent.java` | [`fce5e93`](https://github.com/Terrence721/saga-full/commit/fce5e93) | [#125](https://github.com/Terrence721/saga-full/issues/125) | [#126](https://github.com/Terrence721/saga-full/pull/126) | Done — no findings, merged |
| `src/main/java/.../dto/PaymentStatus.java` | [`fce5e93`](https://github.com/Terrence721/saga-full/commit/fce5e93) | [#127](https://github.com/Terrence721/saga-full/issues/127) | [#128](https://github.com/Terrence721/saga-full/pull/128) | Done — no findings, merged |
| `src/main/java/.../dto/RestaurantApprovedEvent.java` | [`fce5e93`](https://github.com/Terrence721/saga-full/commit/fce5e93) | [#129](https://github.com/Terrence721/saga-full/issues/129) | [#130](https://github.com/Terrence721/saga-full/pull/130) | Done — no findings, merged |
| `src/main/java/.../dto/RestaurantEvent.java` | [`fce5e93`](https://github.com/Terrence721/saga-full/commit/fce5e93) | [#131](https://github.com/Terrence721/saga-full/issues/131) | [#132](https://github.com/Terrence721/saga-full/pull/132) | Done — no findings, merged |
| `src/main/java/.../dto/RestaurantRejectedEvent.java` | [`fce5e93`](https://github.com/Terrence721/saga-full/commit/fce5e93) | [#133](https://github.com/Terrence721/saga-full/issues/133) | [#134](https://github.com/Terrence721/saga-full/pull/134) | Done — no findings, merged |
| `src/main/java/.../repository/InventoryItemRepository.java` | [`2b603b3`](https://github.com/Terrence721/saga-full/commit/2b603b3) | [#135](https://github.com/Terrence721/saga-full/issues/135) | [#136](https://github.com/Terrence721/saga-full/pull/136) | Done — no findings, merged |
| `src/main/java/.../repository/OutboxRepository.java` | [`2b603b3`](https://github.com/Terrence721/saga-full/commit/2b603b3) | [#137](https://github.com/Terrence721/saga-full/issues/137) | [#138](https://github.com/Terrence721/saga-full/pull/138) | Done — no findings, merged |
| `src/main/java/.../repository/RestaurantTicketRepository.java` | [`2b603b3`](https://github.com/Terrence721/saga-full/commit/2b603b3) | [#139](https://github.com/Terrence721/saga-full/issues/139) | [#140](https://github.com/Terrence721/saga-full/pull/140) | Done — no findings, merged |
| `src/main/java/.../service/OutboxPublisherService.java` | [`a5f63e7`](https://github.com/Terrence721/saga-full/commit/a5f63e7) | [#141](https://github.com/Terrence721/saga-full/issues/141) | [#142](https://github.com/Terrence721/saga-full/pull/142) | Done — real finding fixed (unbounded Kafka-send timeout, same as order-service #80/payment-service #104), merged |
| `src/main/java/.../service/RestaurantConsumerConfig.java` | [`a5f63e7`](https://github.com/Terrence721/saga-full/commit/a5f63e7) | [#143](https://github.com/Terrence721/saga-full/issues/143) | [#144](https://github.com/Terrence721/saga-full/pull/144) | Done — real finding fixed (missing DefaultErrorHandler, same as order-service #76/payment-service #106), merged |
| `src/main/java/.../service/RestaurantInventoryService.java` | [`fce5e93`](https://github.com/Terrence721/saga-full/commit/fce5e93) | [#145](https://github.com/Terrence721/saga-full/issues/145) | [#146](https://github.com/Terrence721/saga-full/pull/146) | Done — real finding fixed (unlocked stock deduction race), merged |
| `src/main/java/.../service/RestaurantService.java` | [`fb4fd66`](https://github.com/Terrence721/saga-full/commit/fb4fd66) | [#88](https://github.com/Terrence721/saga-full/issues/88) | [#89](https://github.com/Terrence721/saga-full/pull/89) | Done — covered by `PaymentStatus.java`'s payment-service review (#88), fixed to reject on non-`APPROVED` status instead of blindly running inventory logic, merged early |

**`restaurant-service` module review complete — 18/18 files reviewed, 5 real findings fixed, 0 findings left open** (an unreachable inventory-seeding gap in `InventoryItem.java`, a missing `order_id` unique constraint in `RestaurantTicket.java`, an unbounded Kafka-send timeout in `OutboxPublisherService.java` and a missing `DefaultErrorHandler` in `RestaurantConsumerConfig.java` — both the third occurrence of a gap already fixed in `order-service`/`payment-service` — and an unlocked stock-deduction race in `RestaurantInventoryService.java`). `RestaurantService.java` was covered early under `payment-service`'s `PaymentStatus.java` review (#88), ahead of this module's own audit turn. Full multi-module suite: 126/126 tests passing. See [docs/code-review.md](docs/code-review.md) for the full write-up and [#22](https://github.com/Terrence721/saga-full/issues/22) for the closed tracking issue.

### `api-gateway-service` (module tracking issue [#23](https://github.com/Terrence721/saga-full/issues/23)) — started 2026-08-31

| File | Last commit SHA | Sub-issue | PR | Status |
| - | - | - | - | - |
| `src/main/java/.../ApiGatewayServiceApplication.java` | [`e27264a`](https://github.com/Terrence721/saga-full/commit/e27264a) | [#147](https://github.com/Terrence721/saga-full/issues/147) | [#148](https://github.com/Terrence721/saga-full/pull/148) | Done — no findings, merged |
| `src/main/java/.../config/CommonAppConfig.java` | [`4369f62`](https://github.com/Terrence721/saga-full/commit/4369f62) | [#149](https://github.com/Terrence721/saga-full/issues/149) | [#150](https://github.com/Terrence721/saga-full/pull/150) | Done — no findings in its own code, real bug found deferred to AuthenticationController.java, merged |
| `src/main/java/.../controller/AuthenticationController.java` | [`4369f62`](https://github.com/Terrence721/saga-full/commit/4369f62) | [#151](https://github.com/Terrence721/saga-full/issues/151) | [#152](https://github.com/Terrence721/saga-full/pull/152) | Done — fixed the event-loop-blocking bug found in #149, merged |
| `src/main/java/.../controller/GatewayFallbackController.java` | [`660f24e`](https://github.com/Terrence721/saga-full/commit/660f24e) | [#153](https://github.com/Terrence721/saga-full/issues/153) | [#154](https://github.com/Terrence721/saga-full/pull/154) | Done — added missing test coverage on the real circuit-breaker fallback target, merged |
| `src/main/java/.../dto/AuthRequest.java` | [`4369f62`](https://github.com/Terrence721/saga-full/commit/4369f62) | [#155](https://github.com/Terrence721/saga-full/issues/155) | [#156](https://github.com/Terrence721/saga-full/pull/156) | Done — no findings in its own code, log-injection gap found deferred to UserGrpcClient.java, merged |
| `src/main/java/.../dto/WebTokenResponse.java` | [`4369f62`](https://github.com/Terrence721/saga-full/commit/4369f62) | [#157](https://github.com/Terrence721/saga-full/issues/157) | [#158](https://github.com/Terrence721/saga-full/pull/158) | Done — no findings, merged |
| `src/main/java/.../exception/DependencyUnavailableException.java` | [`4369f62`](https://github.com/Terrence721/saga-full/commit/4369f62) | [#159](https://github.com/Terrence721/saga-full/issues/159) | [#160](https://github.com/Terrence721/saga-full/pull/160) | Done — no findings in its own shape, coverage gap found deferred to GlobalExceptionHandler.java, merged |
| `src/main/java/.../exception/ErrorResponse.java` | [`4369f62`](https://github.com/Terrence721/saga-full/commit/4369f62) | [#161](https://github.com/Terrence721/saga-full/issues/161) | [#162](https://github.com/Terrence721/saga-full/pull/162) | Done — no findings, merged |
| `src/main/java/.../exception/GlobalExceptionHandler.java` | [`4369f62`](https://github.com/Terrence721/saga-full/commit/4369f62) | [#163](https://github.com/Terrence721/saga-full/issues/163) | [#164](https://github.com/Terrence721/saga-full/pull/164) | Done — closed a real coverage gap (the exception-to-HTTP-status matrix wasn't fully exercised end-to-end), added a real running-server integration test proving `@RestControllerAdvice` catches gateway filter-chain errors, merged |
| `src/main/java/.../exception/InvalidCredentialsException.java` | [`4369f62`](https://github.com/Terrence721/saga-full/commit/4369f62) | [#165](https://github.com/Terrence721/saga-full/issues/165) | [#166](https://github.com/Terrence721/saga-full/pull/166) | Done — no findings in its own code, a real structural nuance found in `UserGrpcExceptionTranslator`'s reachability (documented, not a bug), merged |
| `src/main/java/.../exception/UserInactiveException.java` | [`4369f62`](https://github.com/Terrence721/saga-full/commit/4369f62) | [#167](https://github.com/Terrence721/saga-full/issues/167) | [#168](https://github.com/Terrence721/saga-full/pull/168) | Done — no findings, same shape as `InvalidCredentialsException.java`, merged |
| `src/main/java/.../exception/UserNotFoundException.java` | [`4369f62`](https://github.com/Terrence721/saga-full/commit/4369f62) | [#169](https://github.com/Terrence721/saga-full/issues/169) | [#170](https://github.com/Terrence721/saga-full/pull/170) | Done — no findings, same shape as `InvalidCredentialsException.java`/`UserInactiveException.java`, merged |
| `src/main/java/.../filter/JwtPerimeterGuardGatewayFilterFactory.java` | [`660f24e`](https://github.com/Terrence721/saga-full/commit/660f24e) | [#171](https://github.com/Terrence721/saga-full/issues/171) | [#172](https://github.com/Terrence721/saga-full/pull/172) | Done — real finding fixed (CWE-117 log injection via a malformed token's decoded payload), merged |
| `src/main/java/.../infra/grpc/UserGrpcClient.java` | [`4369f62`](https://github.com/Terrence721/saga-full/commit/4369f62) | [#173](https://github.com/Terrence721/saga-full/issues/173) | [#174](https://github.com/Terrence721/saga-full/pull/174) | Done — 2 real findings fixed (CWE-117 log injection, pre-flagged from #155; a zero-test-coverage gap), merged |
| `src/main/java/.../infra/grpc/UserGrpcExceptionTranslator.java` | [`4369f62`](https://github.com/Terrence721/saga-full/commit/4369f62) | [#175](https://github.com/Terrence721/saga-full/issues/175) | [#176](https://github.com/Terrence721/saga-full/pull/176) | Done — no findings in the translation logic, a real test-coverage gap fixed, merged |

**`api-gateway-service` module review complete — 15/15 files reviewed, 6 real findings fixed across 6 files, 0 findings left open** (an event-loop-blocking gRPC login call in `AuthenticationController.java`, a test-coverage gap on the real circuit-breaker fallback target in `GatewayFallbackController.java`, a test-coverage gap across 7 of 9 exception handlers plus a proven-not-assumed `@RestControllerAdvice`/gateway-filter-chain fact in `GlobalExceptionHandler.java`, a CWE-117 log-injection fix in `JwtPerimeterGuardGatewayFilterFactory.java` confirmed by reading auth0's actual `java-jwt` source, a second CWE-117 log-injection fix plus a zero-test-coverage gap in `UserGrpcClient.java` — pre-flagged from `AuthRequest.java`'s own review — and a zero-test-coverage gap in `UserGrpcExceptionTranslator.java`, the last file). Full multi-module suite: 147/147 tests passing. See [docs/code-review.md](docs/code-review.md) for the full write-up and [#23](https://github.com/Terrence721/saga-full/issues/23) for the closed tracking issue.

## 🏆 Code-review audit complete — all 6 modules

Every file across `user-contract`, `user-service`, `order-service`, `payment-service`, `restaurant-service`, and `api-gateway-service` has now gone through this file-by-file review — **73/73 files, 26 real findings fixed across 26 files, 0 findings left open.** See [docs/code-review.md](docs/code-review.md) for the full write-up and [#17](https://github.com/Terrence721/saga-full/issues/17) for the closed parent tracking issue.

**Post-audit findings, same day**: a systematic post-audit sweep (72 main-source classes cross-referenced against every test class, direct or indirect) surfaced 7 more real findings across 7 files that the original per-file review had missed — a test-coverage gap on `CommonAppConfig.java`'s gRPC channel-name wiring, `GrpcExecutor.java`'s untested catch-all safety net, the identical `InterruptedException`-handling gap independently repeated across all three `OutboxPublisherService.java` copies (order/payment/restaurant-service, 3 separate fixes), `RestaurantService.java`'s 4 untested `validate()` null-guards, and one dead constructor removed from `DependencyUnavailableException.java`. All fixed the same way as the audit itself — real issue, real branch, real test, real PR. See [docs/code-review.md](docs/code-review.md#post-audit-findings) for the full write-up.

**Post-audit findings, resumed 2026-09-06**: picked the coverage scan's "not yet re-examined per-branch" list back up. `order-service`'s `OrderController.java`/`OrderConsumerConfig.java`, `payment-service`'s `PaymentConsumerConfig.java`, `restaurant-service`'s `RestaurantConsumerConfig.java`, `GatewayFallbackController.java`, `UserGrpcClient.java`, and `UserGrpcExceptionTranslator.java` (api-gateway-service), plus `UserGrpcServiceImpl.java` (user-service), all checked out clean — every branch, including every configured Kafka non-retryable-exception type and every `UserGrpcExceptionTranslator` status-code case, already has a dedicated test. Two didn't: `AuthenticationController.java`'s `.defaultIfEmpty(...)` branch turned out to be unreachable dead code (`UserGrpcClient.login()` can never return null), removed via [PR #192](https://github.com/Terrence721/saga-full/pull/192); `JwtPerimeterGuardGatewayFilterFactory.java`'s header guard only had its `authHeader == null` half tested, missing the "present but not Bearer-scheme" half, fixed via [PR #194](https://github.com/Terrence721/saga-full/pull/194). Full repo suite: 157/157.

**Coverage scan floor reached, same day**: re-ran the structural cross-reference across all 6 modules — the class list is unchanged from the original 72-class sweep (no new source files since), and every domain/DTO class over 15 lines was spot-checked as still-pure boilerplate with no custom logic. Every class in the repo has now been through either the original 73-file audit or this scan's per-branch pass. Nothing left to check with this method until new code is written — see [docs/code-review.md](docs/code-review.md#post-audit-findings) for the full write-up.

## 🔧 Still to do

| Item | Detail |
| - | - |
| Kubernetes manifests | Deferred until there's a real deployment target to write them for — per-service `Dockerfile`s (Phase 44) now exist to build from, but no manifests yet. The full OTel/Prometheus/Loki/Tempo/Grafana observability stack from the source remains deferred too (Phase 16, reaffirmed for the gateway in Phase 37) |
| Frontend: restaurant register/POS ([#15](https://github.com/Terrence721/saga-full/issues/15)) — **in progress** | This repo has none yet — the source doesn't have one either, it's backend-only. Not a generic storefront: a register/POS-style UI matching the domain the backend already models — a cashier enters an order, watches it move live through kitchen approval (`restaurant-service`) and payment (`payment-service`), landing on `SUCCESS`/`CANCELLED`. **Stack decided 2026-08-31**: React + TypeScript + Vite + Tailwind, calling `api-gateway-service` directly as the single entry point, reusing the existing JWT login flow as-is. **Real-time delivery decided: Server-Sent Events**, not WebSockets or polling — `order-service` streams directly (a `Sinks.Many<Order>` emitted at each status change), `api-gateway-service` just proxies it; the frontend reads it via `fetch()` rather than the native `EventSource` API, which can't set the `Authorization` header the existing `JwtPerimeterGuard` filter requires. v1 scope: one screen, no multi-terminal state, no admin panel, no reporting. **Full 9-step plan approved 2026-09-06; steps 1-5/9 done** — backend order lookup + live SSE stream + gateway routing (Phases 45-48), the Vite/React/TS/Tailwind shell (Phase 50), and a full login flow verified end-to-end against the real running gateway (Phase 51-52, [#215](https://github.com/Terrence721/saga-full/issues/215) still open pending the remaining checklist item below). Remaining: step 6 (order entry, `POST /orders`), step 7 (live status view via the Phase 46 SSE stream), step 8 (Vitest + React Testing Library coverage — no frontend tests exist yet), step 9 (docs) |
| Backend: reservation piece | A real new addition, not yet scoped — no `reservation-service` exists in either this repo or the source. Would need its own module (domain, saga participation, and whatever events tie it into the order/restaurant flow) designed from scratch, the same one-file-at-a-time way as the other services. Its stated trigger — the core order/payment/restaurant saga being done — is now met (Phase 36); still not started or scoped |
